---
phase: 01-data-model-sqlite
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/db/homes.ts
  - src/db/migrate.test.ts
  - src/db/migrate.ts
  - package.json
autonomous: true
requirements:
  - HOME-01
  - HOME-02
must_haves:
  truths:
    - "Developer can insert a home and read back stable integer id (HOME-01) using parameterized SQL."
    - "Developer can insert a user and a home_members row linking them (HOME-02); duplicate membership is rejected."
    - "After migrate, close, reopen on the same file path, membership row still exists (success criteria 3 / D-11)."
  artifacts:
    - path: src/db/homes.ts
      provides: "createHome, createUser, addUserToHome (or equivalent) with bound parameters"
      contains: "prepare|query"
    - path: src/db/migrate.test.ts
      provides: "integration test temp file + reopen"
      contains: "mkdtempSync|tmpdir"
    - path: package.json
      provides: "scripts.test and scripts.migrate"
      contains: "\"test\""
  key_links:
    - from: src/db/migrate.test.ts
      to: src/db/migrate.ts
      via: "migrate(tempPath)"
      pattern: "migrate\\("
    - from: src/db/homes.ts
      to: homes / users / home_members
      via: "INSERT ... $name"
      pattern: "\\$[a-z_]+"
---

<objective>
Expose small parameterized helpers for creating homes, users, and membership; add an integration test that migrates, inserts, closes, reopens, and asserts persistence; wire npm scripts so CI runs migrations without prompts.

Purpose: Delivers ROADMAP success criteria 2–3 and REQUIREMENTS HOME-01, HOME-02 with automated proof (D-11).

Output: `homes.ts`, `migrate.test.ts`, `migrate.ts` (CLI entry or `run-migrate.ts`), updated `package.json`.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-model-sqlite/01-CONTEXT.md
@.planning/codebase/TESTING.md
@src/db/migrate.ts
@src/db/database.ts

## Interfaces (executor: implement these behaviors)

- `migrate(dbFilePath: string)` from `src/db/migrate.ts` must be callable from tests.
- Tables after migration: `users`, `homes`, `home_members`, `schema_migrations` per 01-01.
</context>

<tasks>

<task type="auto">
  <name>Task 1: homes.ts — parameterized createHome, createUser, addUserToHome</name>
  <read_first>
    - src/db/migrate.ts
    - src/db/migrations/001_initial.sql
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  <files>src/db/homes.ts</files>
  <action>
    Add `src/db/homes.ts` exporting typed helpers that accept a `Database` from `bun:sqlite` (import type `Database` from `"bun:sqlite"`):

    - `createUser(db, { displayName: string | null }): number` — `INSERT INTO users (display_name) VALUES ($displayName)` (or equivalent columns), return `Number(db.query("select last_insert_rowid() as id").get().id)` or use `ResultSet.lastInsertRowid` per Bun docs—use one correct API and return integer user id (D-01).
    - `createHome(db, { name: string }): number` — insert into `homes`, return last insert rowid (HOME-01).
    - `addUserToHome(db, { homeId: number, userId: number }): void` — insert into `home_members`; rely on FK + UNIQUE for errors.

    **All dynamic values must use bound parameters** (e.g. `$name`, `$displayName`)—no string interpolation of user-controlled values into SQL strings (mitigate T-1-01).

    Export narrow row types if useful (`interface HomeRow` etc.).
  </action>
  <verify>
    <automated>rg -n "createHome|createUser|addUserToHome" src/db/homes.ts && rg -n "\\$[a-zA-Z]+" src/db/homes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "INSERT INTO users" src/db/homes.ts` exits 0.
    - `rg "INSERT INTO homes" src/db/homes.ts` exits 0.
    - `rg "INSERT INTO home_members" src/db/homes.ts` exits 0.
    - No `db.run(\`...${` pattern: `rg '\$\{' src/db/homes.ts` exits 1 (no template interpolation for SQL).
  </acceptance_criteria>
  <done>HOME-01 and HOME-02 operations are available via typed helpers with parameterized SQL per D-01–D-06.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: migrate.test.ts — migrate, insert, close, reopen, assert + schema integrity</name>
  <read_first>
    - src/db/homes.ts
    - src/db/migrate.ts
    - .planning/codebase/TESTING.md
    - .planning/phases/01-data-model-sqlite/01-CONTEXT.md (D-11)
  </read_first>
  <files>src/db/migrate.test.ts</files>
  <action>
    Create `src/db/migrate.test.ts` using `import { test, expect } from "bun:test"`.

    1. Use `mkdtempSync(join(tmpdir(), "home-assist-test-"))` and a **file path** `join(dir, "persist.sqlite")` — **not** `:memory:` ([schema_push_requirement], D-11).

    2. First connection: `migrate(dbPath)` then `openDatabase(dbPath)` or `new Database(dbPath)` + `PRAGMA foreign_keys=ON`, then `createHome`, `createUser`, `addUserToHome` with known name strings (e.g. home `"Lab"`, user displayName `"Dev"`).

    3. Record `homeId` and `userId` from return values; assert `typeof homeId === "number"` and `> 0` (HOME-01).

    4. `db.close()`.

    5. Second connection: new `Database(dbPath)`, `PRAGMA foreign_keys=ON`, **no migrate call required** (already applied)—optionally call `migrate` again and assert idempotent (no error; schema_migrations unchanged count).

    6. Query `home_members` joined with `homes` and `users` where ids match; expect exactly one row; assert `homes.name` and membership present (HOME-02).

    6b. **Duplicate membership (HOME-02 / UNIQUE):** In a dedicated subtest (separate temp file), run `migrate`, `createHome`, `createUser`, `addUserToHome` once, then call `addUserToHome` again with the **same** `homeId` and `userId`. Expect a thrown error (SQLite `SQLITE_CONSTRAINT` / UNIQUE) — `expect(() => addUserToHome(...)).toThrow()` or equivalent.

    7. Assert FK enforcement: attempt `addUserToHome` with invalid `homeId` or `userId` in a **fresh temp db** in a subtest and `expect(() => ...).toThrow()` or catch constraint error—proves PRAGMA + FK (adjust if SQLite error shape requires `try/catch`).

    8. Assert `schema_migrations` contains version key matching the convention used in `migrate.ts` (e.g. row for `001_initial.sql`).

    Use only parameterized inserts from `homes.ts` for happy path.
  </action>
  <verify>
    <automated>bun test src/db/migrate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/db/migrate.test.ts` exits 0.
    - Test file contains string `mkdtempSync` or `tmpdir` (proves temp dir, not only memory).
    - Test file contains `home_members` and `schema_migrations` substrings.
  </acceptance_criteria>
  <done>[BLOCKING] Schema applied via runner on file-backed DB; persistence across close/reopen verified; FK violation path covered.</done>
</task>

<task type="auto">
  <name>Task 3: package.json scripts — test and migrate (non-interactive)</name>
  <read_first>
    - package.json
    - src/db/migrate.ts
  </read_first>
  <files>package.json, src/db/migrate.ts</files>
  <action>
    Update root `package.json`:

    - Add `"scripts": { "test": "bun test", "migrate": "bun run src/db/run-migrate.ts" }` (or point `migrate` at the actual CLI entry you add).

    Add minimal `src/db/run-migrate.ts` (new file—if you add it, list in files_modified in SUMMARY; **preferred** to avoid extra file: use `bun run src/db/migrate.ts` only if migrate.ts exposes a `if (import.meta.main)` block that calls `migrate(resolveSqlitePath())`).

    **Pick one:** Either extend `migrate.ts` with `import.meta.main` calling `mkdirSync(dirname(resolveSqlitePath()), { recursive: true })` then `migrate(resolveSqlitePath())` from `database.ts`, **or** add `src/db/run-migrate.ts` that imports and invokes—executor must ensure `bun run migrate` applies migrations to default path with no prompts.

    If `run-migrate.ts` is added, `files_modified` for this plan must include it (executor: add to commit and to YAML `files_modified` if not using `import.meta.main` only).

    Do not add interactive prompts.
  </action>
  <verify>
    <automated>cat package.json | rg '"test"' | rg 'bun test' && cat package.json | rg '"migrate"'</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test` exits 0 (same as `bun test`).
    - `bun run migrate` exits 0 against default `data/home-assist.sqlite` (creates `data/` if needed) after migrations; non-TTY safe.
  </acceptance_criteria>
  <done>Discoverable commands for full suite and migration apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test / CLI → SQLite | Inserts use parameterized APIs from `homes.ts`; migration SQL remains static files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | T | homes.ts dynamic SQL | mitigate | Only `prepare`/`query` with named parameters; tests in Task 2 assert no string-built SQL for values. |
| T-1-02 | T | Invalid FK inserts | mitigate | Integration subtest expects failure when FK violated with FK pragma on. |
| T-1-03 | I | data/ committed to git | mitigate | `.gitignore` contains `data/`; `bun run migrate` writes under ignored path by default. |

**High-severity block:** If any helper uses string-concatenated SQL for user fields, stop and fix before merge (SQLi risk for future callers).
</threat_model>

<verification>
1. Run `bun run migrate` once from repo root (creates `data/home-assist.sqlite` if absent); exit 0.
2. Run `bun test` — **must exit 0** with zero failures (final phase gate per [schema_push_requirement]).
3. Confirm ROADMAP success criteria 1–3: (1) migrations + tables exist; (2) test creates home and user membership; (3) data survives close/reopen in `migrate.test.ts`.
</verification>

<success_criteria>
- `bun test` passes for the entire project.
- HOME-01: test asserts stable numeric `home_id` after insert.
- HOME-02: membership row links user and home; uniqueness enforced by schema or error on duplicate.
- D-11: file-backed temp path + second process/connection read confirms row count.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-model-sqlite/01-02-SUMMARY.md`

**Note:** If Task 3 adds `src/db/run-migrate.ts` or `import.meta.main` in `migrate.ts`, update plan frontmatter `files_modified` in SUMMARY to match actual touched files.
</output>
