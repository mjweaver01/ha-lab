---
phase: 01-data-model-sqlite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/001_initial.sql
  - src/db/database.ts
  - src/db/migrate.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
must_haves:
  truths:
    - "Versioned SQL migration 001_initial.sql exists and creates homes, users, home_members, and schema_migrations."
    - "Opening a file-backed Database runs PRAGMA foreign_keys=ON and resolves SQLITE_PATH defaulting to data/home-assist.sqlite."
    - "migrate(dbPath) applies pending migrations in lexical order, records versions in schema_migrations, and wraps each migration in a transaction."
  artifacts:
    - path: src/db/migrations/001_initial.sql
      provides: "DDL for schema_migrations, users, homes, home_members per D-01–D-06"
      contains: "CREATE TABLE homes"
    - path: src/db/database.ts
      provides: "openDatabase(), SQLITE_PATH resolution"
      contains: "foreign_keys"
    - path: src/db/migrate.ts
      provides: "applyMigrations or migrate export"
      contains: "schema_migrations"
  key_links:
    - from: src/db/migrate.ts
      to: src/db/migrations/*.sql
      via: "readDirectory lexical sort"
      pattern: "migrations"
---

<objective>
Add the initial SQLite DDL and a Bun `bun:sqlite` migration runner with foreign keys enabled on every connection.

Purpose: Establishes stable `home_id` / `user_id` tables and idempotent migrations for Phase 2+ (per D-07, D-08, D-09, D-10).

Output: `001_initial.sql`, `database.ts`, `migrate.ts`.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-model-sqlite/01-CONTEXT.md
@.planning/phases/01-data-model-sqlite/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

## Source coverage audit

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Persistent homes and membership; stable IDs | 01-01, 01-02 | COVERED |
| REQ | HOME-01 | Home with stable identifier | 01-01, 01-02 | COVERED |
| REQ | HOME-02 | Users associated with a home | 01-01, 01-02 | COVERED |
| RESEARCH | — | schema_migrations, PRAGMA foreign_keys, transactions | 01-01 | COVERED |
| RESEARCH | — | Integration test temp file (D-11) | 01-02 | COVERED |
| CONTEXT | D-01–D-06 | users, homes, home_members shape | 01-01 | COVERED |
| CONTEXT | D-07–D-08 | Versioned SQL + runner | 01-01 | COVERED |
| CONTEXT | D-09–D-10 | data/home-assist.sqlite, SQLITE_PATH | 01-01 | COVERED |
| CONTEXT | D-11 | bun test persistence | 01-02 | COVERED |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 001_initial.sql with schema_migrations, users, homes, home_members</name>
  <read_first>
    - .planning/phases/01-data-model-sqlite/01-CONTEXT.md (D-01 through D-06)
    - .planning/phases/01-data-model-sqlite/01-RESEARCH.md (Suggested DDL section)
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  <files>src/db/migrations/001_initial.sql</files>
  <action>
    Create `src/db/migrations/001_initial.sql` (lexically first migration; numeric prefix `001_` per D-07).

    Include in order:
    1. `schema_migrations` with column `version TEXT PRIMARY KEY` and optional `applied_at TEXT NOT NULL DEFAULT (datetime('now'))`.
    2. `users`: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `display_name TEXT` nullable (D-02), `created_at TEXT NOT NULL DEFAULT (datetime('now'))` (D-01).
    3. `homes`: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `name TEXT NOT NULL` with `CHECK (length(trim(name)) > 0)`, `created_at TEXT NOT NULL DEFAULT (datetime('now'))` (D-04).
    4. `home_members`: `home_id INTEGER NOT NULL REFERENCES homes(id) ON DELETE CASCADE`, `user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE`, `created_at TEXT NOT NULL DEFAULT (datetime('now'))`, `UNIQUE(home_id, user_id)` (D-05, D-06). Use composite primary key `(home_id, user_id)` or a surrogate—document choice in SQL comment; uniqueness must hold.

    No roles/permissions columns (D-06). Do not add passwords or auth columns (D-02).
  </action>
  <verify>
    <automated>rg -n "CREATE TABLE (schema_migrations|users|homes|home_members)" src/db/migrations/001_initial.sql && rg -n "UNIQUE \\(home_id, user_id\\)" src/db/migrations/001_initial.sql</automated>
  </verify>
  <acceptance_criteria>
    - `rg "CREATE TABLE schema_migrations" src/db/migrations/001_initial.sql` exits 0.
    - `rg "CREATE TABLE homes" src/db/migrations/001_initial.sql` exits 0.
    - `rg "home_members" src/db/migrations/001_initial.sql` exits 0.
    - `rg "REFERENCES homes" src/db/migrations/001_initial.sql` exits 0.
    - `rg "REFERENCES users" src/db/migrations/001_initial.sql` exits 0.
  </acceptance_criteria>
  <done>DDL matches D-01–D-06; grep checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: database.ts — SQLITE_PATH and openDatabase with foreign keys</name>
  <read_first>
    - src/db/migrations/001_initial.sql
    - .planning/phases/01-data-model-sqlite/01-CONTEXT.md (D-09, D-10)
    - tsconfig.json
  </read_first>
  <files>src/db/database.ts</files>
  <action>
    Add `src/db/database.ts` using `import { Database } from "bun:sqlite"` and `import { join } from "node:path"`.

    - Export `function resolveSqlitePath(): string`: if `process.env.SQLITE_PATH` is set and non-empty, return it; else return `join(process.cwd(), "data/home-assist.sqlite")` (D-09). Do not read `.env` manually (Bun loads it).
    - Export `function openDatabase(path?: string): Database`: target path = argument ?? `resolveSqlitePath()`; `new Database(target, { create: true })`; immediately `db.run("PRAGMA foreign_keys = ON;");` (RESEARCH Pattern 1). Return the instance.

    Exported functions must have explicit return types. Use `import type` only where required by verbatimModuleSyntax.
  </action>
  <verify>
    <automated>rg -n "PRAGMA foreign_keys = ON" src/db/database.ts && rg -n "SQLITE_PATH|home-assist\\.sqlite" src/db/database.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "foreign_keys = ON" src/db/database.ts` exits 0.
    - `rg "SQLITE_PATH" src/db/database.ts` exits 0.
    - `rg "home-assist.sqlite" src/db/database.ts` exits 0.
  </acceptance_criteria>
  <done>Every opened DB has FK enforcement on; default path is project-relative `data/home-assist.sqlite` per D-09.</done>
</task>

<task type="auto">
  <name>Task 3: migrate.ts — apply ordered SQL files and record schema_migrations</name>
  <read_first>
    - src/db/database.ts
    - .planning/phases/01-data-model-sqlite/01-RESEARCH.md (Pattern 2: Migration runner)
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  <files>src/db/migrate.ts</files>
  <action>
    Implement `src/db/migrate.ts` that:

    1. Resolves the migrations directory as `join(dirname(fileURLToPath(import.meta.url)), "migrations")` (or equivalent) so it works regardless of cwd.
    2. Lists `*.sql` files, sorts lexicographically ascending (D-07).
    3. Opens `Database(dbFilePath, { create: true })`, runs `PRAGMA foreign_keys = ON` (redundant safety if also using openDatabase—acceptable).
    4. For each migration filename: if `version` not in `schema_migrations`, run `db.transaction(() => { db.run(fileContents); db.run("INSERT INTO schema_migrations (version) VALUES ($version)", { $version: versionKey }); })` where `versionKey` is the filename stem (e.g. `001_initial`). Use the **entire file basename** including `.sql` as version key if simpler—pick one convention and use it consistently in tests (e.g. `001_initial.sql` as version).
    5. Export `export function migrate(dbFilePath: string): void` (or async only if required—prefer sync per bun:sqlite) that performs the above and returns when complete.

    Throw on missing migrations directory, unreadable file, or SQL error.

    Optional: export `export const MIGRATIONS_DIR = ...` for tests—only if needed.

    Do not use string concatenation for migration body into ad-hoc queries from user input—migration SQL is static files only (threat: SQLi N/A for file content; future app code must use parameters—Plan 01-02).
  </action>
  <verify>
    <automated>bun -e "import { migrate } from './src/db/migrate.ts'; import { Database } from 'bun:sqlite'; import { mkdtempSync } from 'node:fs'; import { join } from 'node:path'; import { tmpdir } from 'node:os'; const dir = mkdtempSync(join(tmpdir(), 'ha-plan01-')); const p = join(dir, 'm.db'); migrate(p); const db = new Database(p); db.run('PRAGMA foreign_keys = ON;'); const rows = db.query(\"select name from sqlite_master where type='table' order by name\").all() as { name: string }[]; const names = rows.map(r => r.name); for (const t of ['homes','home_members','schema_migrations','users']) { if (!names.includes(t)) throw new Error('missing table '+t); } const v = db.query('select count(*) as c from schema_migrations').get() as { c: number }; if (v.c < 1) throw new Error('no migrations'); db.close();"</automated>
  </verify>
  <acceptance_criteria>
    - The one-liner `bun -e` above exits 0 (applies migrations to a temp file DB and asserts four tables + schema_migrations row count ≥ 1).
    - `rg "schema_migrations" src/db/migrate.ts` exits 0.
    - `rg "transaction" src/db/migrate.ts` exits 0.
  </acceptance_criteria>
  <done>Pending migrations apply idempotently; re-running migrate on same path does not duplicate schema_migrations rows for the same version.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static SQL files → migration runner | Only trusted repo files are executed as DDL; no runtime user SQL in Phase 1. |
| Env (SQLITE_PATH) → file path | Controls which local file is opened; treat as trusted local dev input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | T | migrate.ts / future queries | mitigate | Use parameterized `db.query` / `prepare` for all dynamic values in Plan 01-02; migration SQL is static files only. |
| T-1-02 | T | FK relationships | mitigate | `PRAGMA foreign_keys = ON` on every connection (database.ts + migrate); FK clauses in DDL. |
| T-1-03 | I | data/ SQLite files | mitigate | `data/` in .gitignore (verify present); document SQLITE_PATH for non-default locations. |
| T-1-04 | S | Local SQLite file | accept | ASVS L1 lab scope; no network or multi-tenant isolation in Phase 1. |

**High-severity block:** None identified for static migrations + local file—mitigations above must be implemented before phase sign-off.
</threat_model>

<verification>
After Plan 01 tasks: `bun -e` smoke in Task 3 passes; grep acceptance criteria pass. Full suite `bun test` is required after Plan 01-02 adds tests.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (schema exists for homes and membership): initial migration + runner in place; tables discoverable after migrate.
- REQ HOME-01 / HOME-02: schema supports homes and membership (behavioral proof in 01-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-model-sqlite/01-01-SUMMARY.md`
</output>
