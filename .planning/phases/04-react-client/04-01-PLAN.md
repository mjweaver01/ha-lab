---
phase: 04-react-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bunfig.toml
  - tsconfig.json
  - src/client/api/events-client.ts
  - src/client/lib/new-events.ts
  - src/client/api/events-client.test.ts
  - src/client/lib/new-events.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
user_setup:
  - service: orchestrator
    why: "Browser client calls Phase 2 GET /events; set base URL for dev"
    env_vars:
      - name: PUBLIC_ORCHESTRATOR_URL
        source: "Literal in bunfig or shell — default http://127.0.0.1:3000 per 04-RESEARCH"
      - name: PUBLIC_HOME_ID
        source: "Optional — numeric home id for list query; default documented in code if unset"
must_haves:
  truths:
    - "GET /events?home_id= is requested via URL + searchParams with encoded home_id"
    - "Event rows are typed for list rendering; body shown as escaped text/JSON (no raw HTML)"
    - "Pure helpers determine which event ids are new since last snapshot (supports UI-02 highlight logic)"
    - "bun test passes for URL client and new-event logic with mocked fetch"
  artifacts:
    - path: "src/client/api/events-client.ts"
      provides: "fetchEvents(baseUrl, homeId), shared EventListItem type (mirror Phase 2 JSON)"
      min_lines: 35
    - path: "src/client/lib/new-events.ts"
      provides: "compare snapshots / max id for marking new arrivals"
      min_lines: 20
    - path: "bunfig.toml"
      provides: "PUBLIC_* env inlining for bun HTML bundler"
  key_links:
    - from: "src/client/api/events-client.ts"
      to: "GET /events?home_id="
      via: "new URL('/events', base) + searchParams.set('home_id', ...)"
      pattern: "/events"
---

<objective>
Add React/react-dom and Bun HTML bundler configuration; implement a typed **`GET /events?home_id=`** client and **pure** “new event” helpers with **`bun:test`** coverage (mocked `fetch`). This establishes the data and diff contract for **UI-01** (list data) and **UI-02** (detecting arrivals) before the visible UI ships in **04-02**.

Purpose: Lock API wiring to Phase 2 (`02-02-PLAN.md`), avoid Vite, and prove polling diff logic without a browser.

Output: `package.json` deps, `bunfig.toml`, extended `tsconfig.json` for DOM, `events-client.ts`, `new-events.ts`, co-located tests.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-react-client/04-RESEARCH.md
@.planning/phases/04-react-client/04-PATTERNS.md
@.planning/phases/04-react-client/04-VALIDATION.md
@.planning/phases/04-react-client/04-UI-SPEC.md
@.planning/phases/02-webhook-orchestrator/02-02-PLAN.md

## Source coverage (phase 4 plan 01)

| Source | Item | Covered |
|--------|------|---------|
| GOAL | React client reads orchestrator events | `fetchEvents` + types |
| REQ | UI-01 | List data via GET /events |
| REQ | UI-02 | `new-events` helpers for highlight/poll diff |
| RESEARCH | Bun HTML `PUBLIC_*`, no Vite | `bunfig.toml`, deps |
| UI-SPEC | Copy not required in this plan | Wired in 04-02 |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies and Bun HTML bundler config</name>
  <read_first>
    - .planning/phases/04-react-client/04-RESEARCH.md (Standard Stack, bunfig, Pitfall 1)
    - .planning/phases/04-react-client/04-PATTERNS.md (package.json, tsconfig JSX)
    - package.json
    - tsconfig.json
  </read_first>
  <files>package.json, bunfig.toml, tsconfig.json</files>
  <action>
Add **`react`** and **`react-dom`** at versions pinned in **04-RESEARCH** (e.g. **19.2.5** — verify with `npm view react version` if needed). Add devDependencies: **`@types/react`**, **`@types/react-dom`**, **`happy-dom`**, **`@testing-library/react`** per RESEARCH.

Create **`bunfig.toml`** with **`[serve.static] env = "PUBLIC_*"`** so literal **`process.env.PUBLIC_ORCHESTRATOR_URL`** / **`PUBLIC_HOME_ID`** inline in bundled client code (per Bun HTML docs — avoid indirect `process.env[key]` access).

Extend root **`tsconfig.json`** **`compilerOptions.lib`** to include **`DOM`** and **`DOM.Iterable`** so `fetch`, `setInterval`, and DOM types resolve under strict mode.

Do **not** add Vite, Express, or `import.meta.env.VITE_*` patterns.

Add **`package.json`** scripts only if needed for this wave (e.g. `"test": "bun test"`); full **`client`** dev script may land in 04-02 with `index.html`.

  </action>
  <verify>
    <automated>bun install && test -f bunfig.toml && rg -q "PUBLIC_\*" bunfig.toml && rg -q "\"react\"" package.json && rg -q "DOM" tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `bun install` completes without peer conflicts.
    - `bunfig.toml` exists and contains `[serve.static]` with `env = "PUBLIC_*"`.
    - `tsconfig.json` includes `DOM` (and `DOM.Iterable` if added) in `lib`.
    - `package.json` lists `react`, `react-dom`, and the dev types/test libs from RESEARCH.
  </acceptance_criteria>
  <done>Deps installed; Bun can inline `PUBLIC_*`; TypeScript sees browser APIs.</done>
</task>

<task type="auto">
  <name>Task 2: events-client + new-event helpers + tests</name>
  <read_first>
    - .planning/phases/02-webhook-orchestrator/02-02-PLAN.md (GET /events?home_id= required)
    - .planning/phases/04-react-client/04-RESEARCH.md (Pattern 3–5, fetch example)
    - src/types/events-api.ts (if present — mirror `PostEventBody` / event row fields; if missing, define client-side `EventListItem` with comment “mirror Phase 2 GET JSON”)
  </read_first>
  <files>src/client/api/events-client.ts, src/client/lib/new-events.ts, src/client/api/events-client.test.ts, src/client/lib/new-events.test.ts</files>
  <action>
**`src/client/api/events-client.ts`**

- Export **`fetchEvents(baseUrl: string, homeId: number): Promise&lt;EventListItem[]&gt;`** using **`new URL("/events", normalizedBase)`** where `normalizedBase` ends with `/` for `URL` resolution, then **`searchParams.set("home_id", String(homeId))`** (numeric id validated: `Number.isFinite(homeId)` and integer — reject or throw before fetch if invalid).
- **`GET`** only; on `!res.ok` throw with message suitable for UI (executor maps to UI-SPEC error copy in 04-02).
- Parse **`res.json()`** as array; if API returns unexpected shape, throw or coerce to `[]` with a clear error — prefer strict array check in tests.
- Define **`EventListItem`** with at least **`id: number`**, **`event_type: string`**, **`created_at: string`** (or `string | number` per landed API), and **`body: unknown`** optional — align field names with **`src/routes/events.ts`** / DB when available.

**`src/client/lib/new-events.ts`**

- Pure functions: e.g. **`maxEventId(events: EventListItem[]): number | undefined`** and **`markNewEventIds(previousMax: number | undefined, events: EventListItem[]): Set&lt;number&gt;`** (or equivalent) so UI-02 can highlight rows whose **`id` &gt; previousMax** after each poll. No `Date`/`setInterval` here — logic only.

**Tests** (`bun:test`):

- Mock **`globalThis.fetch`** to return JSON arrays; assert **`fetchEvents`** calls **`/events?home_id=7`** (example) with **`GET`**.
- Assert **`new-events`** marks ids as new when snapshot grows.
- **Security:** Do **not** use **`dangerouslySetInnerHTML`** anywhere; tests are pure TS — add a comment in **`events-client.ts`** that all API strings are rendered as React text nodes in 04-02.

  </action>
  <verify>
    <automated>bun test src/client/api/events-client.test.ts src/client/lib/new-events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/client/api/events-client.test.ts src/client/lib/new-events.test.ts` exits 0.
    - `rg "searchParams.set\\(\"home_id\"" src/client/api/events-client.ts` exits 0.
    - `rg "dangerouslySetInnerHTML" src/client/api` exits 1 (no matches in client API folder).
    - No `innerHTML` or `document.write` in these modules.
  </acceptance_criteria>
  <done>Typed GET client, new-id set logic, and tests green; ready for React UI in 04-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → orchestrator `GET /events` | JSON treated as untrusted for display |
| Test → mocked `fetch` | Same parsing as production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `event_type`, `body` strings | mitigate | **No `dangerouslySetInnerHTML`** on API-derived strings; React text nodes in 04-02; here: typed `unknown` for `body`, stringify only via `JSON.stringify` in UI inside `<pre>` if needed |
| T-04-02 | I | `home_id` in query | mitigate | Validate **finite integer** `home_id` before building URL; use `searchParams` encoding |
| T-04-03 | E | Non-JSON or huge responses | mitigate | `res.json()` in try/catch; surface error to caller (UI shows UI-SPEC error copy in 04-02) |

</threat_model>

<verification>
- `bun test` passes for new/edited test files.
- Quick grep: no `dangerouslySetInnerHTML` in `src/client/api` or `src/client/lib`.
</verification>

<success_criteria>
- **UI-01:** Client can load event list data from **`GET /events?home_id=`** with correct URL shape.
- **UI-02:** Pure logic exists to determine **new** event ids between polls.
- **04-RESEARCH:** Bun **`PUBLIC_*`** pattern configured; React deps added without Vite.
</success_criteria>

<output>
After completion, create `.planning/phases/04-react-client/04-01-SUMMARY.md`
</output>
