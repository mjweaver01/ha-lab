---
phase: 04-react-client
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - package.json
  - src/client/index.html
  - src/client/main.tsx
  - src/client/events-screen.tsx
  - src/client/hooks/use-events-poll.ts
  - src/client/styles.css
  - src/server.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
user_setup:
  - service: orchestrator
    why: "End-to-end browser test needs Phase 2 running or CORS/same-origin configured"
    env_vars:
      - name: PUBLIC_ORCHESTRATOR_URL
        source: "http://127.0.0.1:3000 (typical Phase 2 PORT)"
      - name: PUBLIC_HOME_ID
        source: "Existing `homes.id` from Phase 1/2 seed data"
must_haves:
  truths:
    - "User sees a single Events screen: heading, optional home id, list of events (type, time, body summary)"
    - "Empty and error states use exact copy from 04-UI-SPEC.md"
    - "Primary control labeled Refresh events triggers a fetch without full page navigation"
    - "Polling runs on a 3–5s configurable interval; new rows use accent (#3d8bfd) per UI-SPEC"
    - "All API strings rendered as JSX text or JSON.stringify in <pre> — never dangerouslySetInnerHTML"
  artifacts:
    - path: "src/client/index.html"
      provides: "Bun HTML entry — script module to main.tsx"
    - path: "src/client/events-screen.tsx"
      provides: "Events list UI + states + dark theme tokens via styles.css"
    - path: "src/client/hooks/use-events-poll.ts"
      provides: "Initial load + interval + in-flight guard + new highlight state"
    - path: "src/client/styles.css"
      provides: "UI-SPEC colors, spacing, typography"
  key_links:
    - from: "use-events-poll.ts"
      to: "fetchEvents"
      via: "imports from ../api/events-client"
    - from: "events-screen.tsx"
      to: "GET /events"
      via: "hook → fetchEvents → state"
---

<objective>
Deliver the **browser-visible** Events screen: Bun **`bun ./src/client/index.html`** (or equivalent) entry, React **`createRoot`**, **`EventsScreen`** matching **04-UI-SPEC.md** (dark theme, copy, spacing), **`use-events-poll`** with **3–5s** polling, **Refresh events** button, and **accent** treatment for new rows — consuming **`fetchEvents`** from **04-01**. Optionally add **CORS** headers on **`src/server.ts`** only if the UI origin differs from the orchestrator and browser `fetch` is blocked.

Purpose: Satisfy **UI-01** (view recent events) and **UI-02** (see new arrivals without full reload) per **REQUIREMENTS.md**.

Output: `index.html`, `main.tsx`, `events-screen.tsx`, `use-events-poll.ts`, `styles.css`, `package.json` script to run the client; conditional **`src/server.ts`** edit for dev CORS.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-react-client/04-UI-SPEC.md
@.planning/phases/04-react-client/04-RESEARCH.md
@.planning/phases/04-react-client/04-PATTERNS.md
@src/client/api/events-client.ts
@src/client/lib/new-events.ts

## Source coverage (phase 4 plan 02)

| Source | Item | Covered |
|--------|------|---------|
| GOAL | Visible event list + live feel | EventsScreen + poll |
| REQ | UI-01 | List + empty state |
| REQ | UI-02 | Interval poll + new highlight |
| UI-SPEC | Tokens, copy, 3–5s poll | styles + copy strings + interval constant |
| RESEARCH | `process.env.PUBLIC_*` | literal base + home id reads in entry or hook |
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML entry, root mount, Events screen + styles</name>
  <read_first>
    - .planning/phases/04-react-client/04-UI-SPEC.md (colors, copy, layout)
    - .planning/phases/04-react-client/04-PATTERNS.md (createRoot, PascalCase .tsx)
    - .planning/phases/04-react-client/04-RESEARCH.md (Pattern 2 standalone `bun ./index.html`)
    - src/client/api/events-client.ts
  </read_first>
  <files>src/client/index.html, src/client/main.tsx, src/client/events-screen.tsx, src/client/styles.css, package.json</files>
  <action>
**`src/client/index.html`:** Single HTML entry with **`<script type="module" src="./main.tsx"></script>`** (path per Bun HTML bundler). Title: e.g. “Home Assist — Events”.

**`src/client/main.tsx`:** Import **`react-dom/client`** **`createRoot`**, import **`./styles.css`**, mount **`<EventsScreen />`** into **`document.getElementById("root")`** — add matching **`<div id="root">`** in HTML.

**`src/client/events-screen.tsx`:** Functional component (no external UI library per UI-SPEC):

- **Layout:** Single column; page bg **`#0f1419`**, row/panel **`#1a2332`**, accent **`#3d8bfd`** for **new** rows only; error **`#e5534b`** for error banner only.
- **Typography/spacing:** Match **04-UI-SPEC** tables (body 16px, heading 20px, label 12px; padding **sm/md/lg** as specified).
- **Copy (exact):** Empty — “No events yet” / “Start the orchestrator and simulator…”; Error — “Could not load events. Check the API URL and that the server is running.”; CTA — “Refresh events”.
- **Data:** For this task, wire **props** or **placeholder state** if hook lands in Task 2 — prefer importing **`fetchEvents`** and **`useEventsPoll`** from Task 2 in the same plan; if Task 1 is strictly presentational first, use temporary local state + **`useEffect`** once for smoke only, then Task 2 replaces with hook. **Recommended:** Implement **EventsScreen** to accept **`events`**, **`error`**, **`loading`**, **`onRefresh`**, **`newIds`**, **`homeId`**, **`pollMs`** so Task 2 only supplies the hook.

- **Rendering:** **`event_type`**, **`created_at`**, **`body`**: use normal JSX **`{value}`** or **`<pre>{JSON.stringify(body, null, 2)}</pre>`** for object bodies. **Never** `dangerouslySetInnerHTML` for API strings.

**`src/client/styles.css`:** Class names for panel, list row, row--new (accent border or left border), empty, error, button, typography — all values traceable to UI-SPEC tokens.

**`package.json`:** Add script **`"client": "bun ./src/client/index.html"`** (or path verified against Bun docs). Ensure **`bun run client`** starts dev bundler.

  </action>
  <verify>
    <automated>test -f src/client/index.html && test -f src/client/main.tsx && rg -q "createRoot" src/client/main.tsx && rg -q "Refresh events" src/client/events-screen.tsx && rg -q "#0f1419" src/client/styles.css && rg -q "\"client\"" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/client/index.html` and `main.tsx` exist; `rg "createRoot" src/client/main.tsx` exits 0.
    - `events-screen.tsx` contains exact UI-SPEC strings for empty, error, and CTA.
    - `styles.css` references dominant/secondary/accent/destructive colors from UI-SPEC.
    - `rg "dangerouslySetInnerHTML" src/client` exits 1.
    - `package.json` includes a `client` script pointing at the HTML entry.
  </acceptance_criteria>
  <done>Static shell and styles match UI-SPEC; XSS-safe string rendering pattern established.</done>
</task>

<task type="auto">
  <name>Task 2: use-events-poll hook, wire Refresh + interval, CORS if needed</name>
  <read_first>
    - src/client/lib/new-events.ts
    - src/client/api/events-client.ts
    - .planning/phases/04-react-client/04-RESEARCH.md (Pattern 4 polling, Pitfall 4 in-flight guard, Pitfall 2 CORS)
    - src/server.ts (if present — for CORS headers on GET /events)
  </read_first>
  <files>src/client/hooks/use-events-poll.ts, src/client/events-screen.tsx, src/client/main.tsx, src/server.ts</files>
  <action>
**`src/client/hooks/use-events-poll.ts`**

- Read **`process.env.PUBLIC_ORCHESTRATOR_URL`** with default **`http://127.0.0.1:3000`** and **`process.env.PUBLIC_HOME_ID`** parsed as integer (fallback e.g. **1** — document lab default; must match a valid **`homes`** row when integrated).
- **`POLL_MS`:** constant **4000** ms (within **3–5s**) or **`process.env.PUBLIC_POLL_MS`** — document in code comment per UI-SPEC “configurable”.
- On mount: initial **`fetchEvents`**; start **`setInterval`** for same; use **`useRef`** **in-flight** flag: skip tick if previous fetch still pending (Pitfall 4).
- Track **`previousMaxId`**; merge **`markNewEventIds`** / **`Set`** from **04-01** so rows with new ids get accent styling for one poll cycle (or until next refresh — choose one behavior and test manually per **04-VALIDATION.md**).
- Expose **`refresh()`** for **Refresh events** button (**no `location.reload()`**).

**Wire `EventsScreen`:** Replace placeholders with hook return values; show loading state as needed (minimal text or aria-busy — no new copy required if not in UI-SPEC).

**CORS (conditional):** If repository has **`src/server.ts`** orchestrator and UI runs on a **different origin/port**, add **`Access-Control-Allow-Origin`** for **development** (e.g. `*` or `http://localhost:5173` — use whatever port `bun ./index.html` uses; Bun default from docs) **only for `GET /events`** responses **or** global OPTIONS — **minimal change**, comment “dev-only”. If **`src/server.ts`** does not exist in this worktree, **skip** file edit and document in SUMMARY that CORS may be required when splitting processes.

  </action>
  <verify>
    <automated>rg -q "setInterval|POLL|4000|3000|5000" src/client/hooks/use-events-poll.ts && rg -q "useEventsPoll|use-events-poll" src/client/events-screen.tsx && bun test</automated>
  </verify>
  <acceptance_criteria>
    - Hook calls **`fetchEvents`** on interval within **3000–5000** ms default (or env override).
    - **Refresh events** triggers **`refresh()`** without full page reload (`rg "location.reload" src/client` exits 1).
    - **`fetchEvents`** import path resolves to **04-01** module.
    - Full suite: **`bun test`** exits 0 (includes 04-01 tests).
    - No **`dangerouslySetInnerHTML`** in **`src/client`**.
  </acceptance_criteria>
  <done>UI-01 list + UI-02 polling and new-row highlight work against running orchestrator when configured; CORS addressed or documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React UI → API strings | All dynamic content via escaped JSX text or `JSON.stringify` |
| Browser → orchestrator | Optional CORS opens read path — lab scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | T | XSS in `event_type` / JSON `body` | mitigate | **Never use `dangerouslySetInnerHTML`** for orchestrator fields; use `<pre>{JSON.stringify(body, null, 2)}</pre>` or string in text node |
| T-04-11 | I | `home_id` from env | mitigate | Parse **`PUBLIC_HOME_ID`** as integer; reject **`NaN`** before fetch |
| T-04-12 | E | CORS `*` in dev | accept (lab) | Document dev-only; production would lock origin + auth (out of scope) |

</threat_model>

<verification>
- `bun test` full suite green.
- Manual (per **04-VALIDATION.md**): run Phase 2 orchestrator + **`bun run client`**, confirm list, refresh, and new-event highlight when simulator posts events.
</verification>

<success_criteria>
- **UI-01:** User can view recent events for a configured home with correct empty/error copy.
- **UI-02:** New events show accent highlight within one poll interval; no full page navigation.
- **04-UI-SPEC:** Visual/copy/token requirements satisfied by inspection.
</success_criteria>

<output>
After completion, create `.planning/phases/04-react-client/04-02-SUMMARY.md`
</output>
