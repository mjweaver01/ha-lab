---
phase: 06-media-signals-events
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/lib/media-throttle.ts
  - src/client/lib/media-signals.ts
  - src/client/lib/media-signals-audio.test.ts
  - src/client/lib/media-signals-video.test.ts
autonomous: true
requirements:
  - MEDIA-03
  - MEDIA-04
must_haves:
  truths:
    - "Audio classifier output can trigger event intents no more than once every 3 seconds while activity continues (D-03)."
    - "Video classifier output can trigger event intents no more than once every 4 seconds while activity continues (D-03)."
    - "Both modalities emit event intents only after accepted classifier results (D-04)."
  artifacts:
    - path: "src/client/lib/media-throttle.ts"
      provides: "Independent accepted-post clocks for audio and video."
    - path: "src/client/lib/media-signals.ts"
      provides: "MediaPipe lifecycle and classifier-to-event intent mapping for both modalities."
    - path: "src/client/lib/media-signals-audio.test.ts"
      provides: "Automated assertions for audio gating + 3s throttle behavior."
    - path: "src/client/lib/media-signals-video.test.ts"
      provides: "Automated assertions for video sampling mapping + 4s throttle behavior."
  key_links:
    - from: "src/client/lib/media-signals.ts"
      to: "src/client/lib/media-throttle.ts"
      via: "accepted result checks before emitting event intent"
      pattern: "canEmit.*markEmitted"
    - from: "src/client/lib/media-signals.ts"
      to: "src/client/lib/media-event-types.ts"
      via: "event type and body mapping"
      pattern: "MEDIA_(AUDIO|VIDEO)_EVENT_TYPE"
---

<objective>
Implement the classifier-driven media signal pipeline for audio and video with independent throttles and deterministic tests before any UI/hook wiring.

Purpose: satisfy D-01 through D-04 in isolated, testable modules so production wiring is low-risk.
Output: reusable throttle primitives plus media signal orchestration functions with modality-specific tests.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-media-signals-events/06-CONTEXT.md
@.planning/phases/06-media-signals-events/06-RESEARCH.md
@.planning/phases/06-media-signals-events/06-PATTERNS.md
@src/client/hooks/use-media-capture.ts
@src/client/lib/media-errors.ts
@src/client/lib/new-events.ts
@src/client/hooks/use-events-poll.ts
@src/client/lib/media-event-types.ts

<interfaces>
From `src/client/hooks/use-media-capture.ts`:

```typescript
videoRef: RefObject<HTMLVideoElement | null>;
startMic: () => Promise<void>;
startCamera: () => Promise<void>;
```

From locked decisions in `06-CONTEXT.md`:

```text
Audio throttle: minimum 3 seconds between accepted posts.
Video throttle: minimum 4 seconds between accepted posts.
event_type values: media.audio / media.video.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build independent accepted-post throttle utility</name>
  <read_first>.planning/phases/06-media-signals-events/06-CONTEXT.md, src/client/hooks/use-events-poll.ts, src/client/lib/new-events.ts</read_first>
  <files>src/client/lib/media-throttle.ts</files>
  <behavior>
    - Test 1: audio clock allows first emit, blocks emits within 3000ms, then allows at/after 3000ms.
    - Test 2: video clock allows first emit, blocks emits within 4000ms, then allows at/after 4000ms.
    - Test 3: marking audio emit does not affect video clock (and vice versa).
  </behavior>
  <action>Create `createMediaThrottleState()` with explicit methods: `canEmitAudio(nowMs)`, `markAudioEmitted(nowMs)`, `canEmitVideo(nowMs)`, `markVideoEmitted(nowMs)`. Store separate timestamps and only update in `mark*` methods so throttling is tied to accepted emissions (D-04). Use constants `AUDIO_MIN_INTERVAL_MS = 3000` and `VIDEO_MIN_INTERVAL_MS = 4000` (D-03).</action>
  <acceptance_criteria>- `rg "AUDIO_MIN_INTERVAL_MS\\s*=\\s*3000" src/client/lib/media-throttle.ts` returns one match.
- `rg "VIDEO_MIN_INTERVAL_MS\\s*=\\s*4000" src/client/lib/media-throttle.ts` returns one match.
- `rg "canEmitAudio|markAudioEmitted|canEmitVideo|markVideoEmitted" src/client/lib/media-throttle.ts` returns all four method names.</acceptance_criteria>
  <verify>
    <automated>rg "AUDIO_MIN_INTERVAL_MS|VIDEO_MIN_INTERVAL_MS|canEmitAudio|markAudioEmitted|canEmitVideo|markVideoEmitted" src/client/lib/media-throttle.ts</automated>
  </verify>
  <done>Throttle module enforces independent 3s/4s accepted-post cadence with deterministic behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MediaPipe signal mapping module and modality tests</name>
  <read_first>.planning/phases/06-media-signals-events/06-CONTEXT.md, .planning/phases/06-media-signals-events/06-RESEARCH.md, src/client/lib/media-event-types.ts, src/client/lib/media-throttle.ts, src/client/hooks/use-media-capture.ts</read_first>
  <files>src/client/lib/media-signals.ts, src/client/lib/media-signals-audio.test.ts, src/client/lib/media-signals-video.test.ts</files>
  <behavior>
    - Test 1: accepted audio classification above threshold emits `media.audio` intent with JSON-safe body and respects 3s throttle.
    - Test 2: accepted video classification above threshold emits `media.video` intent with JSON-safe body and respects 4s throttle.
    - Test 3: below-threshold or empty classifier outputs emit nothing and do not update throttle clocks.
  </behavior>
  <action>Create `src/client/lib/media-signals.ts` with explicit API surface:
  `type MediaSignalEvent = { event_type: "media.audio" | "media.video"; body: unknown }`,
  `type EmitMediaSignalEvent = (event: MediaSignalEvent) => Promise<void> | void`,
  `createMediaSignalPipeline({ homeId, emit, nowMs, audioThreshold, videoThreshold })`.
  Implement `handleAudioClassification(result)` and `handleVideoClassification(result)` to map top classifier candidate to shared body helpers, then gate through `media-throttle` before calling `emit`. Include comments documenting the inspectable video sampling expectation (D-02) and that posting is classifier-driven (D-01).</action>
  <acceptance_criteria>- `rg "createMediaSignalPipeline" src/client/lib/media-signals.ts` returns one match.
- `rg "handleAudioClassification|handleVideoClassification" src/client/lib/media-signals.ts` returns both handlers.
- `rg "media\\.audio|media\\.video" src/client/lib/media-signals.ts` confirms modality event types.
- `rg "3s|3000|4s|4000" src/client/lib/media-signals.ts` confirms documented cadence rules.
- `rg "below-threshold|emit nothing|throttle" src/client/lib/media-signals-audio.test.ts src/client/lib/media-signals-video.test.ts` shows explicit gating/throttle assertions.</acceptance_criteria>
  <verify>
    <automated>bun test src/client/lib/media-signals-audio.test.ts src/client/lib/media-signals-video.test.ts</automated>
  </verify>
  <done>Signal module converts classifier results into modality-specific, throttled event intents with passing audio/video tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MediaPipe inference output → app logic | dynamic model output enters event-mapping pipeline |
| signal pipeline → event emission callback | repeated modality activity can create high request pressure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | D (Denial of Service) | `src/client/lib/media-throttle.ts` and `src/client/lib/media-signals.ts` | mitigate | Enforce independent 3000ms/4000ms accepted-emission intervals and update clocks only after accepted emits (D-03, D-04). |
| T-06-05 | T (Tampering) | `src/client/lib/media-signals.ts` | mitigate | Emit events only from classifier-driven handlers (D-01) and central constants/body builders (D-05, D-06), avoiding ad-hoc payload construction. |
| T-06-06 | I (Information Disclosure) | `src/client/lib/media-signals.ts` | mitigate | Normalize classifier output into small JSON-safe summaries; do not emit raw media buffers, streams, or task internals. |
</threat_model>

<verification>
- `bun test src/client/lib/media-signals-audio.test.ts src/client/lib/media-signals-video.test.ts`
- `bun test`
</verification>

<success_criteria>
- Audio and video signal logic is classifier-driven and independently throttled (D-01 through D-04).
- Modality mappings remain centralized with inspectable output semantics (D-05, D-06).
- Unit tests prove accepted-only throttling and suppression of low-confidence noise.
</success_criteria>

<output>
After completion, create `.planning/phases/06-media-signals-events/06-02-SUMMARY.md`
</output>
