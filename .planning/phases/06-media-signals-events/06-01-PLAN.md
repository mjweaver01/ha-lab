---
phase: 06-media-signals-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/client/api/events-client.ts
  - src/client/api/events-client.post.test.ts
  - src/client/lib/media-event-types.ts
autonomous: true
requirements:
  - MEDIA-03
  - MEDIA-04
must_haves:
  truths:
    - "Client can build `PostEventBody` payloads for both media modalities without schema drift."
    - "Audio and video payload naming stays stable and centralized for inspectable event tracing."
  artifacts:
    - path: "src/client/lib/media-event-types.ts"
      provides: "Centralized `media.audio` / `media.video` constants and JSON-safe body builders (per D-05, D-06)."
    - path: "src/client/api/events-client.ts"
      provides: "Typed `postEvent` helper for `POST /events` using locked `PostEventBody` shape."
    - path: "src/client/api/events-client.post.test.ts"
      provides: "Automated checks for method/url/body/error behavior of `postEvent`."
  key_links:
    - from: "src/client/lib/media-event-types.ts"
      to: "src/client/api/events-client.ts"
      via: "shared payload fields and event_type constants"
      pattern: "MEDIA_(AUDIO|VIDEO)_EVENT_TYPE"
    - from: "src/client/api/events-client.ts"
      to: "/events"
      via: "fetch POST with JSON content type"
      pattern: "method:\\s*\"POST\""
---

<objective>
Create the client-side event contract layer and POST transport so both media modalities can emit valid `PostEventBody` payloads to `/events` with one shared source of truth.

Purpose: enforce locked naming/body decisions before signal loops are wired, preventing drift across audio and video paths.
Output: installed MediaPipe packages, new media event constants/body helpers, and tested `postEvent` API client support.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-media-signals-events/06-CONTEXT.md
@.planning/phases/06-media-signals-events/06-RESEARCH.md
@src/types/events-api.ts
@src/client/api/events-client.ts
@scripts/simulated-node.ts

<interfaces>
From `src/types/events-api.ts`:

```typescript
export type PostEventBody = {
  home_id: number;
  event_type: string;
  body?: unknown;
};
```

From `src/client/api/events-client.ts`:

```typescript
export async function fetchEvents(
  baseUrl: string,
  homeId: number,
  fetchImpl: typeof fetch = globalThis.fetch.bind(globalThis),
): Promise<EventListItem[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add locked media event contract module and install MediaPipe packages</name>
  <read_first>.planning/phases/06-media-signals-events/06-CONTEXT.md, .planning/phases/06-media-signals-events/06-RESEARCH.md, src/types/events-api.ts, package.json</read_first>
  <files>package.json, bun.lock, src/client/lib/media-event-types.ts</files>
  <action>Run `bun add @mediapipe/tasks-audio @mediapipe/tasks-vision`. Create `src/client/lib/media-event-types.ts` exporting `MEDIA_AUDIO_EVENT_TYPE = "media.audio"` and `MEDIA_VIDEO_EVENT_TYPE = "media.video"` (per D-05), plus JSON-safe helpers for classifier metadata (per D-06): `type MediaCandidate = { label: string; score: number }`, `type MediaEventBody = { top_label: string; top_score: number; candidates?: MediaCandidate[]; source: "audio" | "video" }`, and builder functions that clamp `score` into `[0,1]`, trim labels, and return plain-object values only.</action>
  <acceptance_criteria>- `rg "MEDIA_AUDIO_EVENT_TYPE\\s*=\\s*\"media\\.audio\"" src/client/lib/media-event-types.ts` returns one match.
- `rg "MEDIA_VIDEO_EVENT_TYPE\\s*=\\s*\"media\\.video\"" src/client/lib/media-event-types.ts` returns one match.
- `rg "type MediaEventBody" src/client/lib/media-event-types.ts` returns one match.
- `rg "@mediapipe/tasks-audio|@mediapipe/tasks-vision" package.json` confirms both dependencies are present.</acceptance_criteria>
  <verify>
    <automated>bun test src/client/api/events-client.test.ts</automated>
  </verify>
  <done>Shared constants/body builders exist and MediaPipe dependencies are installed without breaking existing client API tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and test typed `postEvent` transport helper</name>
  <read_first>src/client/api/events-client.ts, src/types/events-api.ts, scripts/simulated-node.ts, src/client/api/events-client.test.ts</read_first>
  <files>src/client/api/events-client.ts, src/client/api/events-client.post.test.ts</files>
  <behavior>
    - Test 1: `postEvent(baseUrl, payload)` issues `POST /events` with `Content-Type: application/json` and serialized `PostEventBody`.
    - Test 2: non-2xx responses surface error text and status in the thrown message.
    - Test 3: fetch/network failures are wrapped as `Could not reach orchestrator: ...`.
  </behavior>
  <action>Create `src/client/api/events-client.post.test.ts` first (RED), then add `postEvent(baseUrl, payload, fetchImpl?)` in `events-client.ts` with: `assertValidHomeId(payload.home_id)`, non-empty trimmed `event_type` validation, URL construction via `new URL("/events", normalizeBaseUrl(baseUrl))`, `POST` method, JSON body, and consistent error wrapping style already used by `fetchEvents`.</action>
  <acceptance_criteria>- `rg "export async function postEvent" src/client/api/events-client.ts` returns one match.
- `rg "method:\\s*\"POST\"" src/client/api/events-client.ts` returns one match within `postEvent`.
- `rg "new URL\\(\"/events\"" src/client/api/events-client.ts` returns one match in `postEvent`.
- `rg "describe\\(|test\\(" src/client/api/events-client.post.test.ts` shows tests for success and error paths.</acceptance_criteria>
  <verify>
    <automated>bun test src/client/api/events-client.test.ts src/client/api/events-client.post.test.ts</automated>
  </verify>
  <done>Client can post valid `PostEventBody` payloads with robust error handling and automated transport tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → `/events` API | untrusted client payload crosses into orchestrator ingestion |
| classifier metadata → JSON request body | dynamic inference output is normalized before transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | `src/client/api/events-client.ts` | mitigate | Validate `home_id` integer and non-empty `event_type` before POST; enforce single payload builder module to reduce schema drift. |
| T-06-02 | D (Denial of Service) | `src/client/api/events-client.ts` | mitigate | Keep POST transport isolated; rate limiting/throttle enforcement is implemented in Plan 06-02 before hook wiring in 06-03. |
| T-06-03 | I (Information Disclosure) | `src/client/lib/media-event-types.ts` | mitigate | Ensure body builders emit plain JSON-safe summary fields only (label/score/candidates), never raw streams or DOM/media objects. |
</threat_model>

<verification>
Run focused client API tests, then confirm no regressions:

- `bun test src/client/api/events-client.test.ts src/client/api/events-client.post.test.ts`
- `bun test`
</verification>

<success_criteria>
- `MEDIA-03` and `MEDIA-04` contract prerequisites are in place: stable event names and typed POST helper.
- `media.audio` and `media.video` naming exists in one shared module (D-05).
- JSON-safe classifier body conventions are centralized and reusable (D-06).
</success_criteria>

<output>
After completion, create `.planning/phases/06-media-signals-events/06-01-SUMMARY.md`
</output>
