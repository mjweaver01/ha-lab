---
phase: 06-media-signals-events
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/client/hooks/use-media-capture.ts
  - src/client/media-capture-section.tsx
  - src/server.ts
  - src/orchestrator.integration.test.ts
autonomous: true
requirements:
  - MEDIA-03
  - MEDIA-04
must_haves:
  truths:
    - "Live mic activity can flow through classifier output into throttled `POST /events` payloads with `event_type: media.audio`."
    - "Live camera activity can flow through inspectable frame sampling into throttled `POST /events` payloads with `event_type: media.video`."
    - "Browser-originated `POST /events` succeeds via explicit CORS preflight/response headers in dev."
  artifacts:
    - path: "src/client/hooks/use-media-capture.ts"
      provides: "Lifecycle wiring from capture streams to media signal pipeline and `postEvent` transport."
    - path: "src/server.ts"
      provides: "CORS support for OPTIONS/GET/POST on `/events`."
    - path: "src/orchestrator.integration.test.ts"
      provides: "Automated preflight + POST path assertions."
  key_links:
    - from: "src/client/hooks/use-media-capture.ts"
      to: "src/client/lib/media-signals.ts"
      via: "start/stop lifecycle and camera sampling loop"
      pattern: "createMediaSignalPipeline"
    - from: "src/client/hooks/use-media-capture.ts"
      to: "src/client/api/events-client.ts"
      via: "postEvent call with home_id/event_type/body"
      pattern: "postEvent\\("
    - from: "src/server.ts"
      to: "src/orchestrator.integration.test.ts"
      via: "OPTIONS/POST CORS behavior"
      pattern: "Access-Control-Allow-Methods"
---

<objective>
Wire the new media signal pipeline into live capture flow and harden `/events` CORS behavior so browser POSTs from the client dev origin are reliably accepted.

Purpose: complete end-to-end Phase 6 behavior from real browser streams to orchestrator ingestion without server flooding or preflight failures.
Output: integrated hook wiring plus integration-tested CORS support for browser POST to `/events`.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-media-signals-events/06-CONTEXT.md
@.planning/phases/06-media-signals-events/06-RESEARCH.md
@.planning/phases/06-media-signals-events/06-PATTERNS.md
@.planning/phases/05-local-media-capture/05-02-SUMMARY.md
@src/client/hooks/use-media-capture.ts
@src/client/media-capture-section.tsx
@src/client/api/events-client.ts
@src/client/lib/media-signals.ts
@src/server.ts
@src/orchestrator.integration.test.ts

<interfaces>
From `src/client/hooks/use-media-capture.ts`:

```typescript
export function useMediaCapture(): {
  micActive: boolean;
  cameraActive: boolean;
  micError: string | null;
  cameraError: string | null;
  startMic: () => Promise<void>;
  startCamera: () => Promise<void>;
  videoRef: RefObject<HTMLVideoElement | null>;
}
```

From `src/server.ts`:

```typescript
if (path === "/events" && req.method === "OPTIONS") { ... }
if (path === "/events" && req.method === "POST") { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire media signal pipeline into capture hook lifecycle</name>
  <read_first>src/client/hooks/use-media-capture.ts, src/client/lib/media-signals.ts, src/client/api/events-client.ts, src/client/lib/public-env.ts, .planning/phases/06-media-signals-events/06-CONTEXT.md</read_first>
  <files>src/client/hooks/use-media-capture.ts, src/client/media-capture-section.tsx</files>
  <behavior>
    - Test 1: starting mic/camera initializes signal pipeline and enabling capture triggers modality-specific emit callbacks.
    - Test 2: stopping mic/camera tears down classifier loops/resources; no emissions continue after stop.
    - Test 3: MediaPipe init/classification failure sets non-silent user-facing errors consistent with existing media error panel.
  </behavior>
  <action>In `use-media-capture.ts`, instantiate `createMediaSignalPipeline` with explicit values: `audioThrottleMs=3000`, `videoThrottleMs=4000`, `event_type` values from `media-event-types`, and `emit` callback that calls `postEvent(orchestratorBaseUrl, { home_id, event_type, body })`. Use `PUBLIC_HOME_ID` and `PUBLIC_ORCH_BASE_URL` from existing env helpers. Start audio classification when mic starts, start documented video frame sampling loop when camera starts (include comment with sample cadence in ms per D-02), and stop all pipeline timers/tasks when respective streams stop/unmount. Surface pipeline failures through `setMicError`/`setCameraError`; ensure `media-capture-section.tsx` continues rendering alerts without markup changes that hide errors.</action>
  <acceptance_criteria>- `rg "createMediaSignalPipeline" src/client/hooks/use-media-capture.ts` returns one match.
- `rg "postEvent\\(" src/client/hooks/use-media-capture.ts` returns one match.
- `rg "3000|4000" src/client/hooks/use-media-capture.ts` confirms explicit modality throttle values.
- `rg "sample|cadence|frame" src/client/hooks/use-media-capture.ts` finds comment documenting video sampling inspectability (D-02).
- `rg "events-error\" role=\"alert" src/client/media-capture-section.tsx` still matches existing error rendering.</acceptance_criteria>
  <verify>
    <automated>bun test src/client/hooks/use-media-capture.test.ts src/client/lib/media-signals-audio.test.ts src/client/lib/media-signals-video.test.ts</automated>
  </verify>
  <done>Capture hook emits throttled media events via classifier-driven pipeline and preserves user-visible failure behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enable `/events` CORS for browser POST and lock with integration tests</name>
  <read_first>src/server.ts, src/routes/events.ts, src/orchestrator.integration.test.ts, .planning/phases/06-media-signals-events/06-RESEARCH.md</read_first>
  <files>src/server.ts, src/orchestrator.integration.test.ts</files>
  <behavior>
    - Test 1: `OPTIONS /events` returns `204` and `Access-Control-Allow-Methods` includes `POST`.
    - Test 2: `POST /events` responses include CORS headers used by browser clients.
    - Test 3: existing ingest/list integration flow remains green after CORS updates.
  </behavior>
  <action>Update `corsDevHeaders` in `src/server.ts` so `Access-Control-Allow-Methods` includes `GET, POST, OPTIONS`. Wrap `/events` POST responses with `withCorsDev(res, req)` (same pattern used for GET) so browser fetch receives CORS headers on success/error. Extend `src/orchestrator.integration.test.ts` with explicit preflight assertion (`OPTIONS /events`) and response-header checks after `POST /events`.</action>
  <acceptance_criteria>- `rg "Access-Control-Allow-Methods\", \"GET, POST, OPTIONS\"" src/server.ts` returns one match.
- `rg "return withCorsDev\\(res, req\\)" src/server.ts` shows POST path wrapped with CORS helper.
- `rg "OPTIONS" src/orchestrator.integration.test.ts` finds explicit preflight test call.
- `rg "Access-Control-Allow-Methods|Access-Control-Allow-Origin" src/orchestrator.integration.test.ts` shows header assertions.</acceptance_criteria>
  <verify>
    <automated>bun test src/orchestrator.integration.test.ts src/client/api/events-client.post.test.ts</automated>
  </verify>
  <done>Browser preflight and POST responses for `/events` satisfy CORS requirements with integration coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser media hook → orchestrator API | untrusted browser environment sends event payloads over HTTP |
| Browser origin → orchestrator CORS layer | preflight policy controls which cross-origin methods are accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | D (Denial of Service) | `src/client/hooks/use-media-capture.ts` | mitigate | Integrate only the throttled signal pipeline from Plan 06-02; ensure emit loops stop when capture stops or component unmounts. |
| T-06-08 | T (Tampering) | `src/server.ts` CORS policy | mitigate | Explicitly allow only required methods (`GET, POST, OPTIONS`) and continue routing through existing body parsing/validation in `handlePostEvent`. |
| T-06-09 | S (Spoofing) | `POST /events` `home_id` | accept | Existing server behavior already rejects unknown home IDs (`404 home not found`); Phase 6 does not add auth/session scope. |
</threat_model>

<verification>
- `bun test src/client/hooks/use-media-capture.test.ts src/client/lib/media-signals-audio.test.ts src/client/lib/media-signals-video.test.ts`
- `bun test src/orchestrator.integration.test.ts src/client/api/events-client.post.test.ts`
- `bun test`
</verification>

<success_criteria>
- Real mic and camera capture paths are wired to classifier-driven, throttled `POST /events` emissions.
- `/events` preflight and POST responses include correct CORS headers for browser clients.
- Phase 6 requirements (`MEDIA-03`, `MEDIA-04`) are implementation-complete and test-backed, excluding Phase 7 UI list confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-media-signals-events/06-03-SUMMARY.md`
</output>
