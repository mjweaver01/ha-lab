---
phase: 08-locations-hub-and-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/locations-api.ts
  - src/db/migrations/003_locations_lifecycle.sql
  - src/db/locations.ts
  - src/routes/locations.ts
  - src/server.ts
  - src/routes/locations.test.ts
autonomous: true
requirements:
  - LOC-01
  - LOC-02
  - LOC-03
  - LOC-04
  - LOC-05
must_haves:
  truths:
    - "Authorized users receive only location rows they can access."
    - "Create and edit enforce required name and structured optional metadata."
    - "Archive and restore are soft lifecycle transitions, never hard delete."
    - "Active-only is default while archived can be explicitly included."
  artifacts:
    - path: "src/db/migrations/003_locations_lifecycle.sql"
      provides: "Location lifecycle schema with archived_at semantics"
      contains: "CREATE TABLE locations"
    - path: "src/routes/locations.ts"
      provides: "GET/POST/PATCH/archive/restore handlers with access checks"
      exports: ["handleGetLocations", "handlePostLocation", "handlePatchLocation", "handleArchiveLocation", "handleRestoreLocation"]
    - path: "src/routes/locations.test.ts"
      provides: "Route-level enforcement tests for LOC-01..LOC-05"
      contains: "describe(\"locations routes\""
  key_links:
    - from: "src/server.ts"
      to: "src/routes/locations.ts"
      via: "routeRequest dispatch branches"
      pattern: "path === \"/locations\""
    - from: "src/routes/locations.ts"
      to: "src/db/locations.ts"
      via: "repository calls for scoped reads/writes"
      pattern: "listLocationsForUser|createLocation|updateLocation"
---

<objective>
Establish the server-side location lifecycle contract (schema, repository, routes, and tests) that enforces access scope and archive-first behavior.

Purpose: Deliver LOC-01..LOC-05 foundations with explicit non-leaky access control per D-11 and D-12.
Output: New location schema + route surface consumed by the client hub in later plans.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-locations-hub-and-lifecycle/08-CONTEXT.md
@.planning/phases/08-locations-hub-and-lifecycle/08-RESEARCH.md
@.planning/phases/08-locations-hub-and-lifecycle/08-PATTERNS.md
@src/routes/events.ts
@src/db/migrations/001_initial.sql
@src/db/migrations/002_events_subscribers.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create location data contracts and lifecycle schema</name>
  <read_first>.planning/phases/08-locations-hub-and-lifecycle/08-CONTEXT.md, src/types/events-api.ts, src/db/migrations/001_initial.sql, src/db/migrations/002_events_subscribers.sql</read_first>
  <files>src/types/locations-api.ts, src/db/migrations/003_locations_lifecycle.sql, src/db/locations.ts, src/routes/locations.test.ts</files>
  <acceptance_criteria>
    1) `src/types/locations-api.ts` exports `LocationListItem`, `CreateLocationBody`, `UpdateLocationBody`, and `LocationLifecycleAction`.
    2) `src/db/migrations/003_locations_lifecycle.sql` contains `CREATE TABLE locations`, `archived_at TEXT`, and an index containing `WHERE archived_at IS NULL`.
    3) `src/db/locations.ts` exports `listLocationsForUser`, `createLocation`, `updateLocation`, `archiveLocation`, and `restoreLocation`.
    4) `src/routes/locations.test.ts` exists as a Wave 0 scaffold with a `describe("locations routes"` block so route-task verification can run before full lifecycle test expansion.
  </acceptance_criteria>
  <action>Create `src/types/locations-api.ts` with exact payload keys: `name` (required string), optional `code`, optional `notes`, and list fields `id`, `name`, `code`, `notes`, `archived_at`, `updated_at`. Add `src/db/migrations/003_locations_lifecycle.sql` to create `locations` table (`id`, `name`, `code`, `notes`, `created_at`, `updated_at`, `archived_at`, `archived_by`, `archive_reason`) and `location_members` table (`location_id`, `user_id`, `created_at`) with foreign keys to `users`, plus `idx_locations_active` partial index (`WHERE archived_at IS NULL`) per D-03/D-04/D-09/D-10. Implement `src/db/locations.ts` repository functions using prepared statements and user-scoped predicates (`JOIN location_members ON location_id`, filter by `user_id`, include archived controlled by boolean) per D-11. Also add `src/routes/locations.test.ts` as a minimal scaffold (`describe("locations routes", ...)`) so Task 2 targeted verify has an executable test file before Task 3 expands full coverage.</action>
  <verify>
    <automated>bun test src/db/migrate.test.ts -t "applies all migrations once"</automated>
  </verify>
  <done>Schema and repository APIs exist and support active-default + include-archived behavior without any hard-delete function.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement locations route handlers and server dispatch</name>
  <read_first>.planning/phases/08-locations-hub-and-lifecycle/08-CONTEXT.md, src/routes/events.ts, src/server.ts, src/db/locations.ts</read_first>
  <files>src/routes/locations.ts, src/server.ts</files>
  <acceptance_criteria>
    1) `src/routes/locations.ts` exports handlers for GET `/locations`, POST `/locations`, PATCH `/locations/:id`, POST `/locations/:id/archive`, POST `/locations/:id/restore`.
    2) Access-denied responses return JSON with `error: "access denied"` and no location identifiers in message text.
    3) `src/server.ts` route dispatch includes all five locations branches and preserves existing events/subscribers routing.
  </acceptance_criteria>
  <action>Add `src/routes/locations.ts` following `json()`/parse/guard pattern from `src/routes/events.ts`. Parse actor via `x-user-id` header as integer; return `400` for missing/invalid actor header. Implement `GET /locations?include_archived=0|1` defaulting to active-only (D-04), `POST /locations` and `PATCH /locations/:id` using one validation contract for name/code/notes (D-06/D-08), and lifecycle handlers for archive/restore using `archived_at` transitions only (D-03/D-05). Add dispatch in `src/server.ts` for `/locations` and `/locations/:id/*` methods with CORS behavior matching existing `/events` handling.</action>
  <verify>
    <automated>bun test src/routes/locations.test.ts -t "locations routes"</automated>
  </verify>
  <done>Locations routes enforce non-leaky access scope and soft-lifecycle transitions with stable JSON response contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add route-level tests for lifecycle and scope boundaries</name>
  <read_first>.planning/phases/08-locations-hub-and-lifecycle/08-RESEARCH.md, src/orchestrator.integration.test.ts, src/routes/events.ts, src/routes/locations.ts</read_first>
  <files>src/routes/locations.test.ts</files>
  <acceptance_criteria>
    1) Test file includes cases named: `default active list`, `include archived`, `create location`, `edit location`, `archive location`, `restore location`, `access denied`.
    2) Tests assert archive operation keeps row present in DB with non-null `archived_at`.
    3) Tests assert unauthorized actor cannot read or mutate target location and receives `403`.
  </acceptance_criteria>
  <action>Create `src/routes/locations.test.ts` with temporary SQLite server fixture mirroring `src/orchestrator.integration.test.ts`. Seed two users and membership rows in `location_members`; assert LOC-01 list scope, LOC-02 create, LOC-03 edit, LOC-04 archive (soft-only), LOC-05 include-archived toggle, and D-12 non-leaky 403 payloads. Include one negative test confirming that inaccessible location IDs are not returned in list JSON.</action>
  <verify>
    <automated>bun test src/routes/locations.test.ts</automated>
  </verify>
  <done>Automated route tests cover all backend lifecycle and access behaviors required for Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (`/locations*`) | Untrusted request body, query params, and actor header cross into privileged data operations |
| API→SQLite | Route inputs are transformed into SQL predicates and writes |
| lifecycle transition boundary | Archive/restore mutates visibility state used by downstream reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I | `GET /locations`, `PATCH /locations/:id` | mitigate | Enforce membership join by `x-user-id`; return `403 {"error":"access denied"}` without exposing target identifiers (D-11, D-12) |
| T-08-02 | T | create/edit payload parsing | mitigate | Whitelist keys (`name`, `code`, `notes`) and reject invalid body/unknown shape with 400 |
| T-08-03 | T | SQL query construction | mitigate | Use prepared statements and bound parameters only in `src/db/locations.ts` |
| T-08-04 | R | archive/restore endpoints | mitigate | Persist `archived_at`, optional `archived_by`, `archive_reason`; verify transitions in tests |
| T-08-05 | I | active list filtering | mitigate | Default predicate `archived_at IS NULL` unless explicit `include_archived=1` |
</threat_model>

<verification>
Run `bun test src/db/migrate.test.ts src/routes/locations.test.ts` and verify all tests pass.
</verification>

<success_criteria>
Server can create, edit, archive, restore, and list locations with access scoping and active-default visibility, fully covered by automated route tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-locations-hub-and-lifecycle/08-01-SUMMARY.md`
</output>
