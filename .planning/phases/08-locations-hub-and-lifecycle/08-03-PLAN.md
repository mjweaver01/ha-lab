---
phase: 08-locations-hub-and-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/client/main.tsx
  - src/client/main.locations-nav.test.tsx
autonomous: true
requirements:
  - VIEW-01
must_haves:
  truths:
    - "User can enter locations hub from app shell and open a selected location detail."
    - "Hub and detail transitions preserve stable screen state after refresh-level remount."
    - "Navigation regression tests protect row/open path behavior."
  artifacts:
    - path: "src/client/main.tsx"
      provides: "App-level screen routing between events, locations hub, and location detail/edit"
      contains: "useState<\"events\" | \"settings\" | \"locations\" | \"location-detail\">"
    - path: "src/client/main.locations-nav.test.tsx"
      provides: "Navigation tests for hub->detail and back"
      contains: "describe(\"locations navigation\""
  key_links:
    - from: "src/client/main.tsx"
      to: "src/client/locations-screen.tsx"
      via: "screen switch render branch"
      pattern: "screen === \"locations\""
    - from: "src/client/main.tsx"
      to: "src/client/location-detail-screen.tsx"
      via: "selectedLocationId state"
      pattern: "selectedLocationId"
---

<objective>
Wire locations hub and detail surfaces into the app shell so Phase 8 navigation is complete and regression-tested.

Purpose: Close VIEW-01 at integration level by proving end-to-end in-client navigation flows.
Output: App shell route state and tests guaranteeing hub/detail transitions.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-locations-hub-and-lifecycle/08-CONTEXT.md
@.planning/phases/08-locations-hub-and-lifecycle/08-UI-SPEC.md
@src/client/main.tsx
@src/client/locations-screen.tsx
@src/client/location-detail-screen.tsx
@src/client/events-screen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add app-shell screen routing for locations hub and detail</name>
  <read_first>.planning/phases/08-locations-hub-and-lifecycle/08-CONTEXT.md, src/client/main.tsx, src/client/locations-screen.tsx, src/client/location-detail-screen.tsx</read_first>
  <files>src/client/main.tsx, src/client/main.locations-nav.test.tsx</files>
  <acceptance_criteria>
    1) `main.tsx` screen union includes `locations` and `location-detail`.
    2) App state includes `selectedLocationId` and passes it into detail surface.
    3) A visible control from existing app shell can open locations hub from default screen.
    4) Back navigation from detail returns to hub without resetting include-archived state object in memory.
    5) `src/client/main.locations-nav.test.tsx` exists with an `open location detail` case so Task 1 verification can execute before Task 2 broadens coverage.
  </acceptance_criteria>
  <action>Update `src/client/main.tsx` to orchestrate four screens: `events`, `settings`, `locations`, and `location-detail`. Add `selectedLocationId: number | null` state. Wire `EventsScreen` secondary action to open locations hub, `LocationsScreen` callbacks to open detail by id and to create new location via detail/edit surface, and `LocationDetailScreen` back action to return to hub. Keep existing media settings flow untouched while adding this new branching. Create `src/client/main.locations-nav.test.tsx` scaffold with an `open location detail` navigation assertion so Task 1 verify references a file created in the same task before Task 2 expands the suite.</action>
  <verify>
    <automated>bun test src/client/main.locations-nav.test.tsx -t "open location detail"</automated>
  </verify>
  <done>App shell supports stable hub/detail navigation paths required by VIEW-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add navigation integration tests for hub and detail transitions</name>
  <read_first>src/client/events-screen.test.tsx, src/client/main.tsx, .planning/phases/08-locations-hub-and-lifecycle/08-UI-SPEC.md</read_first>
  <files>src/client/main.locations-nav.test.tsx</files>
  <acceptance_criteria>
    1) Test file asserts default screen, transition to locations hub, transition to detail via row/open action, and back-to-hub behavior.
    2) Test verifies selected location id passed to detail surface matches clicked row id.
    3) Test verifies navigation still works after component rerender (simulated refresh-level remount).
  </acceptance_criteria>
  <action>Create `src/client/main.locations-nav.test.tsx` with `happy-dom` setup and component mocking for `EventsScreen`, `LocationsScreen`, and `LocationDetailScreen`. Assert sequence: app starts on events, user opens locations hub, user triggers open-location action with id `42`, detail view renders id `42`, user goes back to hub, and open action remains available. Include one rerender assertion to ensure navigation state rehydrates correctly.</action>
  <verify>
    <automated>bun test src/client/main.locations-nav.test.tsx</automated>
  </verify>
  <done>Navigation integration coverage protects VIEW-01 from regressions at app-shell level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-shell state boundary | Screen selection and selected location id gate which data/controls are reachable |
| user interaction→navigation handler | Click targets route user to scoped detail views |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | I | app-shell navigation state | mitigate | Only accept location ids originating from hub row selection callbacks; do not parse arbitrary ids from uncontrolled strings |
| T-08-11 | E | screen routing controls | mitigate | Keep privileged mutations in server layer; app-shell routing only changes view state and relies on backend access checks |
| T-08-12 | R | navigation behavior regressions | mitigate | Add integration tests covering open/back/remount transitions to produce repeatable evidence for VIEW-01 |
</threat_model>

<verification>
Run `bun test src/client/main.locations-nav.test.tsx` and confirm hub/detail transitions and selected location propagation pass.
</verification>

<success_criteria>
App shell can consistently navigate users from hub into a specific location detail and back with automated regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/08-locations-hub-and-lifecycle/08-03-SUMMARY.md`
</output>
