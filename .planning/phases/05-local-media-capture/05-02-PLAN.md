---
phase: 05-local-media-capture
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/client/media-capture-section.tsx
  - src/client/media-capture-section.test.tsx
  - src/client/events-screen.tsx
  - src/client/styles.css
autonomous: true
requirements:
  - MEDIA-01
  - MEDIA-02
user_setup: []
must_haves:
  truths:
    - Events page shows a collapsible **Media capture** section with independent Start/Stop for mic and camera per UI-SPEC copy table.
    - When mic is on, user sees **Microphone on** and a visible level meter; when off, **Microphone off** and meter not showing active capture.
    - When camera is on, user sees **Camera on** and a live preview within max height 192px; when off, **Camera off** and no live preview.
    - Errors use `role="alert"` and `events-error` class (or same visual rules) with UI-SPEC strings.
  artifacts:
    - src/client/media-capture-section.tsx
    - src/client/media-capture-section.test.tsx
    - src/client/events-screen.tsx
    - src/client/styles.css
  key_links:
    - `EventsScreen` renders `MediaCaptureSection` between meta line and toolbar (or between toolbar and errors — choose one and document in SUMMARY).
---

<objective>
Add the **Media capture** collapsible UI (per **D-01** and **05-UI-SPEC**), wire **useMediaCapture**, and style with existing CSS tokens. Integrate into `EventsScreen` so the feature is reachable on the main Events page.

Purpose: Deliver visible capture controls and all roadmap UX success criteria for Phase 5.

Output: New component + stylesheet additions + tests for alert role and button labels.
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI ↔ hook | Component only renders hook state; no additional network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-04 | Information disclosure | Preview video | accept | Preview is local-only; no canvas export to network in Phase 5. |
| T-5-05 | XSS | `media-capture-section.tsx` | mitigate | Use React text nodes for errors; no `dangerouslySetInnerHTML` for user or API strings. |
</threat_model>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-local-media-capture/05-UI-SPEC.md
@.planning/phases/05-local-media-capture/05-CONTEXT.md
@src/client/events-screen.tsx
@src/client/styles.css
@src/client/hooks/use-media-capture.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MediaCaptureSection component + styles</name>
  <files>src/client/media-capture-section.tsx, src/client/media-capture-section.test.tsx, src/client/styles.css</files>
  <read_first>
    - .planning/phases/05-local-media-capture/05-UI-SPEC.md
    - src/client/styles.css
    - src/client/hooks/use-media-capture.ts
  </read_first>
  <action>
    Create `src/client/media-capture-section.tsx` that calls `useMediaCapture()` and renders:
    - A collapsible region: prefer native `&lt;details&gt;` / `&lt;summary&gt;` with visible summary text **Media capture**, or a button with `aria-expanded` + `aria-controls` linking to a panel `id` — must satisfy **D-01** discoverability.
    - Per device: **Start microphone** / **Stop microphone** and **Start camera** / **Stop camera** labels exactly as UI-SPEC (or Stop/Start pairing consistent with spec table).
    - Status lines: `Microphone on` / `Microphone off`, `Camera on` / `Camera off`.
    - Mic level meter: horizontal bar, min width 120px, height 8px, fill reflects `micLevel` when mic on.
    - `&lt;video>` with `ref={videoRef}`, `playsInline`, `muted`, `autoPlay`, `aria-label="Camera preview"` when camera active; CSS max-height **192px**, width 100%, `object-fit: cover`, border-radius using `--space-sm`.
    - When `micError` or `cameraError` set, render `&lt;div className="events-error" role="alert"&gt;` with the error string (may show one or both — prefer latest or combined with clear labels).
    Add classes under `styles.css` (e.g. `media-capture`, `media-capture__meter`, `media-capture__video`) using only existing tokens.
    Create `media-capture-section.test.tsx` with `@testing-library/react` + happy-dom: render component with mocked hook (mock module `use-media-capture` or `vi.mock` pattern available in bun) asserting **Start microphone** and `role="alert"` when error injected.
  </action>
  <verify>
    <automated>bun test src/client/media-capture-section.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Media capture" src/client/media-capture-section.tsx` matches.
    - `grep -n "events-error" src/client/media-capture-section.tsx` matches.
    - `grep -n "media-capture" src/client/styles.css` matches.
    - `bun test src/client/media-capture-section.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Component matches UI-SPEC dimensions and copy; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into EventsScreen</name>
  <files>src/client/events-screen.tsx</files>
  <read_first>
    - src/client/events-screen.tsx
    - src/client/media-capture-section.tsx
  </read_first>
  <action>
    Import and render `MediaCaptureSection` in `EventsScreen` after the `events-page__meta` paragraph and before the toolbar (`events-toolbar`), so media is discoverable without scrolling past the list.
    Pass no props if hook is internal to section (preferred). Ensure list and refresh flow unchanged.
  </action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MediaCaptureSection" src/client/events-screen.tsx` matches.
    - `bun test` exits 0.
  </acceptance_criteria>
  <done>Events page shows Media capture section; full suite green.</done>
</task>

</tasks>

<verification>
Run `bun test` full suite. Manually open `bun run client` and confirm expand/collapse and Start/Stop (checker cannot automate OS permissions).
</verification>

<success_criteria>
- [ ] Roadmap success criteria 1–4 for Phase 5 achievable via manual UAT after this plan
- [ ] `bun test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-local-media-capture/05-02-SUMMARY.md`
</output>
