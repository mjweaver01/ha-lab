---
phase: 03-simulated-node
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/simulated-node.ts
  - scripts/simulated-node.test.ts
  - package.json
autonomous: true
requirements:
  - NODE-01
user_setup: []
must_haves:
  truths:
    - "Developer can run a documented `bun run` command that POSTs JSON to `{ORCHESTRATOR_URL}/events`."
    - "Each POST body uses Phase 2 field names: `home_id`, `event_type`, optional `body` (same shape as `PostEventBody`)."
    - "At least two distinct `event_type` values are emitted in a default run (motion, door) and a camera stub is included (e.g. `camera.stub`)."
    - "Script uses Web `fetch` with `Content-Type: application/json` and exits non-zero when the response is not OK."
  artifacts:
    - path: "scripts/simulated-node.ts"
      provides: "CLI + exported payload helpers + POST via fetch"
      min_lines: 80
    - path: "scripts/simulated-node.test.ts"
      provides: "URL normalization + payload field-name tests"
      min_lines: 40
    - path: "package.json"
      provides: "`simulate` script invoking the simulator"
      contains: "\"simulate\""
  key_links:
    - from: "scripts/simulated-node.ts"
      to: "process.env.ORCHESTRATOR_URL"
      via: "base URL for POST /events"
      pattern: "ORCHESTRATOR_URL"
    - from: "scripts/simulated-node.ts"
      to: "`/events`"
      via: "normalize base + append path"
      pattern: "/events"
---

<objective>
Deliver a Bun TypeScript CLI under `scripts/simulated-node.ts` that simulates a hardware node by POSTing sample events to the Phase 2 orchestrator at `POST /events`, using JSON bodies that match **Phase 2 `PostEventBody`**: `home_id` (number), `event_type` (string), optional `body` (JSON-serializable object). Wire `bun run simulate` (or equivalent) in `package.json`, read **`ORCHESTRATOR_URL`** from the environment, and prove behavior with **`bun:test`** (URL builder + field names + mocked `fetch`).

Purpose: Enables local end-to-end lab flow without devices (NODE-01).

Output: `scripts/simulated-node.ts`, `scripts/simulated-node.test.ts`, updated `package.json`.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-simulated-node/03-RESEARCH.md
@.planning/phases/03-simulated-node/03-PATTERNS.md
@.planning/phases/03-simulated-node/03-VALIDATION.md
@.planning/REQUIREMENTS.md

**Locked contract (Phase 2 — do not rename):** POST `/events` JSON body fields **`home_id`**, **`event_type`**, optional **`body`** — same as:

```typescript
interface PostEventBody {
  home_id: number;
  event_type: string;
  body?: unknown;
}
```

(Source: `.planning/phases/02-webhook-orchestrator/02-RESEARCH.md`.)

<interfaces>
Export the above shape from `scripts/simulated-node.ts` (or a re-export comment pointing to this plan) so executors and tests share one definition.

**Sample `event_type` strings (concrete defaults):** `"motion"`, `"door"`, `"camera.stub"` — third is the camera stub; default CLI run must emit all three so NODE-01’s “motion, door, camera stub” is satisfied in one invocation.

**URL:** `fetch(\`${normalizeBaseUrl(ORCHESTRATOR_URL)}/events\`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) })` with `normalizeBaseUrl` stripping exactly one trailing `/` from the base (e.g. `http://127.0.0.1:3000/` → `http://127.0.0.1:3000`).
</interfaces>
</context>

## Source coverage (audit)

| Source | Item | Plan coverage |
|--------|------|----------------|
| GOAL | Simulated node POSTs realistic events without hardware | Tasks 1–2 |
| REQ | NODE-01 | Tasks 1–2 (`requirements` frontmatter) |
| RESEARCH | `fetch`, `ORCHESTRATOR_URL`, `/events`, ≥2 types + camera stub | Tasks 1–2 |
| LOCK | `home_id`, `event_type`, `body` = `PostEventBody` | Task 1 payloads + tests |

---

<tasks>

<task type="auto">
  <name>Task 1: Payload contract, URL helpers, and unit tests</name>
  <files>scripts/simulated-node.ts, scripts/simulated-node.test.ts</files>

  <read_first>
  - `.planning/phases/02-webhook-orchestrator/02-RESEARCH.md` — `PostEventBody` (`home_id`, `event_type`, optional `body`).
  - `.planning/phases/03-simulated-node/03-PATTERNS.md` — kebab-case `scripts/`, `verbatimModuleSyntax`, `bun:test` import style.
  - `.planning/codebase/TESTING.md` — `import { test, expect } from "bun:test"`.
  </read_first>

  <action>
  In **`scripts/simulated-node.ts`** (create `scripts/` if missing):

  1. **`export interface PostEventBody`** with exactly: `home_id: number`, `event_type: string`, `body?: unknown` (matches Phase 2 lock).

  2. **Exported helpers** (pure, testable):
     - `export function normalizeOrchestratorBaseUrl(base: string): string` — trim whitespace; if empty throw `Error("ORCHESTRATOR_URL or --url is required")`; **reject** schemes other than `http:` and `https:` with a clear `Error` (SSRF hygiene per 03-RESEARCH security note).
     - `export function stripTrailingSlashOnce(base: string): string` — if `base.endsWith("/")` then slice off one `/` only (handles `http://a/` → `http://a`, not multi-slash edge cases beyond single trailing slash).
     - `export function eventsPostUrl(base: string): string` — `stripTrailingSlashOnce(normalizeOrchestratorBaseUrl(base)) + "/events"` (no `//` before `events`).

  3. **Sample payload factories** (all return `PostEventBody` with **snake_case JSON keys inside `body`** where applicable for realism):
     - `sampleMotion(home_id: number)` → `event_type: "motion"`, `body: { zone: "driveway", confidence: 0.92 }`
     - `sampleDoor(home_id: number)` → `event_type: "door"`, `body: { door_id: "front", state: "closed" }`
     - `sampleCameraStub(home_id: number)` → `event_type: "camera.stub"`, `body: { clip_id: "demo-001", reason: "motion" }`

  4. **`export function allSampleEvents(home_id: number): PostEventBody[]`** — `[sampleMotion, sampleDoor, sampleCameraStub]` in that order (proves ≥2 distinct `event_type` values and includes camera stub).

  In **`scripts/simulated-node.test.ts`**:

  - Assert `eventsPostUrl("http://127.0.0.1:3000/") === "http://127.0.0.1:3000/events"`.
  - Assert `eventsPostUrl("https://example.com") === "https://example.com/events"`.
  - Assert `allSampleEvents(1).map((e) => e.event_type)` contains **`"motion"`**, **`"door"`**, **`"camera.stub"`** and **at least 3 elements**.
  - For the first element of `allSampleEvents(42)`, `expect` **`home_id === 42`**, **`"home_id" in JSON.parse(JSON.stringify(e))`** is redundant — instead assert serializing preserves keys: `JSON.stringify(e)` includes `"home_id":42` and `"event_type":` (grep-friendly shape).
  - Test that `normalizeOrchestratorBaseUrl("file:///etc/passwd")` throws (or equivalent invalid scheme rejection).

  Do **not** add `fetch` or `main` in this task yet if splitting; if implementing in one pass with Task 2, same files may gain `postEvent` in Task 2 only — **minimum for Task 1:** helpers + tests green; optionally stub `postEvent` in Task 2.

  **Concrete values to use in code:** default numeric `home_id` for samples in tests: **`42`** and **`1`** as needed; motion zone string **`"driveway"`**; door_id **`"front"`**; camera clip_id **`"demo-001"`**.
  </action>

  <acceptance_criteria>
  - `bun test /Users/michaelweaver/Websites/home-assist/scripts/simulated-node.test.ts` exits **0**.
  - `grep -E 'home_id|event_type' /Users/michaelweaver/Websites/home-assist/scripts/simulated-node.ts` shows both field names in the **exported** contract or literals.
  - `grep -c 'camera.stub' /Users/michaelweaver/Websites/home-assist/scripts/simulated-node.ts` is **≥ 1**.
  </acceptance_criteria>

  <verify>
    <automated>bun test /Users/michaelweaver/Websites/home-assist/scripts/simulated-node.test.ts</automated>
  </verify>

  <done>
  `PostEventBody` and sample factories use **`home_id` / `event_type` / `body`** only; URL helper produces a single `/events` path without `//`; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: fetch POST, CLI entry, package.json script, exit codes</name>
  <files>scripts/simulated-node.ts, package.json</files>

  <read_first>
  - package.json (scripts section before edit)
  - `.planning/phases/03-simulated-node/03-RESEARCH.md` — `fetch` POST JSON, non-zero on HTTP errors, `util.parseArgs` + `Bun.argv`.
  - `.planning/codebase/CONVENTIONS.md` — try/catch at CLI boundary, avoid logging secrets.
  - Task 1 completed file `scripts/simulated-node.ts` (extend, do not rename exported helpers).
  </read_first>

  <action>
  1. **`export async function postEvent(baseUrl: string, payload: PostEventBody): Promise&lt;Response&gt;`** — `fetch(eventsPostUrl(baseUrl), { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) })`. Do **not** log full `payload` or raw URLs that may contain userinfo; on failure log **HTTP status** and **safe** short message (e.g. first 200 chars of `response.text()` only on error path, or status line only — see threat model).

  2. **CLI** at bottom of `scripts/simulated-node.ts` (guard with `import.meta.main` per Bun convention **or** always-run main when executed as script — use the project’s standard: if no `import.meta.main`, use `if (process.argv[1]?.includes("simulated-node"))` is fragile; **prefer** `if (import.meta.main) { await main(); }` documented in Bun).

  3. **`util.parseArgs`** from `node:util` with `args: Bun.argv`, **options:**
     - `url: { type: "string" }` — optional override of base URL (**not** the full `/events` path).
     - `home: { type: "string" }` — optional; default **`"1"`**; parse with `Number(values.home ?? "1")` and reject non-integer **home_id** for v1 with a clear error (e.g. `Number.isInteger` after parse).

  4. **Resolve base URL:** `const base = values.url ?? process.env.ORCHESTRATOR_URL ?? ""` then `normalizeOrchestratorBaseUrl(base)` (empty throws from helper).

  5. **Default behavior:** POST **`allSampleEvents(home_id)`** sequentially with `await`; if any `!response.ok`, **`console.error`** minimal diagnostic, **`process.exit(1)`** after first failure.

  6. **`package.json`**: add **`"scripts"`** if missing; add **`"simulate": "bun run scripts/simulated-node.ts"`** (exact path). Preserve existing keys (`name`, `module`, `type`, etc.).

  **Concrete defaults:** `ORCHESTRATOR_URL=http://127.0.0.1:3000` in docs/comments only; CLI default **`--home` omitted → `1`**; three POSTs in order motion → door → camera.stub.
  </action>

  <acceptance_criteria>
  - `grep -F 'ORCHESTRATOR_URL' /Users/michaelweaver/Websites/home-assist/scripts/simulated-node.ts` exits **0**.
  - `grep -E '"simulate"|simulate' /Users/michaelweaver/Websites/home-assist/package.json` matches **`simulate`** with **`bun run scripts/simulated-node.ts`**.
  - `grep -E 'method:\s*["'\'']POST["'\'']|method: "POST"' /Users/michaelweaver/Websites/home-assist/scripts/simulated-node.ts` exits **0** (proves `fetch` uses POST).
  - `bun test /Users/michaelweaver/Websites/home-assist/scripts/simulated-node.test.ts` still exits **0** after adding **`mock`**ed `fetch` test: one test calls `postEvent` with `globalThis.fetch` mocked to return `{ ok: true, status: 201, text: async () => "" }` and asserts `fetch` was called with URL ending in **`/events`** and `init.method === "POST"` and `JSON.parse(init.body).home_id` defined (extend test file).
  </acceptance_criteria>

  <verify>
    <automated>bun test /Users/michaelweaver/Websites/home-assist/scripts/simulated-node.test.ts</automated>
  </verify>

  <done>
  `bun run simulate` runs the script; with `ORCHESTRATOR_URL` set and Phase 2 up, three POSTs succeed; on 4xx/5xx the process exits **1**. No secrets written to logs (see threat model).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev machine → orchestrator | Script sends attacker-controlled URL only via `ORCHESTRATOR_URL` / `--url`; treat as trusted lab input with scheme restriction. |
| Logs → terminal | Untrusted response bodies could contain sensitive data; limit what is printed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | `console.error` on failed POST | mitigate | Log **HTTP status** and optionally truncated error text; **never** log full request `body`, env secrets, or `Authorization` headers. Do not log **`ORCHESTRATOR_URL`** if it may embed credentials (prefer logging only `new URL(base).origin` or status line). |
| T-03-02 | Spoofing / SSRF | `ORCHESTRATOR_URL` / `--url` | mitigate | `normalizeOrchestratorBaseUrl` allows **only** `http:` and `https:` (Task 1). |
| T-03-03 | Tampering | JSON payload | accept | Lab-only; orchestrator validates. |

</threat_model>

<verification>
- Full automated suite: `bun test` from repo root.
- Manual smoke (document in executor summary, not blocking automation): start Phase 2 orchestrator, `ORCHESTRATOR_URL=http://127.0.0.1:<port> bun run simulate`, confirm events in storage/logs per Phase 2.
</verification>

<success_criteria>
- NODE-01 satisfied: script emits motion, door, and camera stub without hardware; uses **`fetch`** POST; **`ORCHESTRATOR_URL`** documented in code path; **`bun run simulate`** exists.
- JSON field names **`home_id`**, **`event_type`**, optional **`body`** match **`PostEventBody`** / Phase 2 lock everywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulated-node/03-01-SUMMARY.md` per execute-plan workflow.
</output>

## PLANNING COMPLETE

**Phase:** 03-simulated-node  
**Plans:** 1 plan in 1 wave  

### Wave structure

| Wave | Plans | Autonomous |
|------|-------|------------|
| 1 | 03-01 | yes |

### Plans created

| Plan | Objective | Tasks | Files |
|------|-----------|-------|-------|
| 03-01 | Simulated node CLI + tests + package script | 2 | `scripts/simulated-node.ts`, `scripts/simulated-node.test.ts`, `package.json` |

### Next steps

Execute: `/gsd-execute-phase 03-simulated-node`

<sub>`/clear` first — fresh context window</sub>
