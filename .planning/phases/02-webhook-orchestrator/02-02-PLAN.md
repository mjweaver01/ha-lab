---
phase: 02-webhook-orchestrator
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/server.ts
  - src/routes/events.ts
  - src/routes/subscribers.ts
  - src/webhooks/fan-out.ts
  - src/webhooks/fan-out.test.ts
  - src/db/database.ts
  - package.json
  - index.ts
  - src/orchestrator.integration.test.ts
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04
user_setup: []
must_haves:
  truths:
    - "POST `/events` with JSON `PostEventBody` returns 201 (or 200) with persisted `event` id when `home_id` exists"
    - "GET `/events` (or equivalent query route) returns stored events for a `home_id`"
    - "POST subscriber registration persists `callback_url` scoped by `home_id`"
    - "After POST `/events`, fan-out attempts run for every subscriber of that home; two subscribers ⇒ two `fetch` attempts (mocked) and two `event_deliveries` rows"
  artifacts:
    - path: "src/server.ts"
      provides: "Bun.serve on PORT default 3000, SQLITE_PATH"
      min_lines: 25
    - path: "src/orchestrator.integration.test.ts"
      provides: "Integration tests ephemeral port + mock fetch"
      min_lines: 40
  key_links:
    - from: "src/routes/events.ts"
      to: "src/webhooks/fan-out.ts"
      via: "after INSERT into events"
      pattern: "fanOut|fan-out"
    - from: "src/webhooks/fan-out.ts"
      to: "global fetch"
      via: "POST JSON to callback_url"
      pattern: "fetch\\("
---

<objective>
Implement the Bun HTTP orchestrator: **`Bun.serve`** with `PORT` (default **3000**) and `SQLITE_PATH` from Phase 1, **`POST /events`** using locked **`PostEventBody`**, subscriber registration, **`GET`** query for events, and **fan-out** with `Promise.allSettled`, **`fetch` mocked in tests** to prove HOOK-04 without external network dependencies.

Purpose: Delivers HOOK-01–HOOK-04 end-to-end with observable delivery records.

Output: Runnable server module, route handlers, fan-out service, integration tests with ephemeral port and mocked `fetch`.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-webhook-orchestrator/02-RESEARCH.md
@.planning/phases/02-webhook-orchestrator/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

**Locked JSON contract:** `POST /events` body — `home_id: number`, `event_type: string`, optional `body: unknown` (`PostEventBody` from `src/types/events-api.ts`).

**Test strategy (chosen — concrete acceptance):** Use **`bun:test` `mock()`** to replace `globalThis.fetch`. **Acceptance:** For one POST `/events` with two subscribers registered for the same `home_id`, the test **asserts `fetch` was invoked exactly twice** with `method: "POST"` and JSON body containing the **new event id** (and expected fields); optionally assert **two rows** in `event_deliveries` with `status` reflecting attempt outcome. **Do not** require live echo servers for the gate (avoids port races); optional extra test with local `Bun.serve` echo is nice-to-have only.

**Server:** `Bun.serve({ port: Number(Bun.env.PORT ?? process.env.PORT ?? 3000), ... })` — use one consistent env read; document `SQLITE_PATH` for DB path.

## Source coverage (wave 2)

| Source | Item | Covered by this plan |
|--------|------|----------------------|
| GOAL | Single Bun service: ingest, persist, fan-out | server + routes + fan-out |
| REQ | HOOK-01–HOOK-04 | All four behaviors + tests |
| RESEARCH | Bun.serve routes, fetch fan-out, allSettled, deliveries table | Implementation + mock test |
| CONTEXT | D-04 FK `homes.id` | Inserts use valid `home_id` only; 404/400 on missing home |

<interfaces>
<!-- Import these types in handlers — do not redefine. -->

From `src/types/events-api.ts` (plan 02-01):

```typescript
export type PostEventBody = {
  home_id: number;
  event_type: string;
  body?: unknown;
};
```

Subscriber registration body (exported alongside in same file):

```typescript
export type PostSubscriberBody = {
  home_id: number;
  callback_url: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fan-out service + delivery persistence</name>
  <read_first>
    - .planning/phases/02-webhook-orchestrator/02-RESEARCH.md (fan-out, event_deliveries)
    - src/types/events-api.ts
    - src/db/migrations/002_events_subscribers.sql
  </read_first>
  <files>src/webhooks/fan-out.ts, src/webhooks/fan-out.test.ts, src/db/database.ts</files>
  <action>Implement `fan-out.ts` that:

- Accepts `Database` (or thin query helpers), **`event_id`**, **`home_id`**.
- Loads all **`subscribers`** for `home_id` via **parameterized** SQL (`WHERE home_id = $homeId`).
- For each subscriber, **`fetch(callback_url, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) })`** where `payload` includes at least `{ event_id, home_id, event_type, body }` (include fields needed for subscribers; align with stored event row).
- Uses **`Promise.allSettled`** so one failure does not block others.
- After each attempt, **INSERT into `event_deliveries`** with `status`, `http_status` (from `Response.status` if ok branch), `error_text` (from thrown/rejected reason or non-ok body truncated).

**SQLi:** Only parameterized queries — no string concatenation of user input into SQL.

Export a single async function e.g. `deliverEventToSubscribers(db, eventRow): Promise&lt;void&gt;`.

Wire **no floating promises**: callers `await` this function or handle in `try/catch`.

**Response policy (lock for tests):** **Await `allSettled` before returning HTTP response** for POST `/events` so integration tests can assert deliveries without races (acceptable for lab; document in code comment).

Add **`src/webhooks/fan-out.test.ts`:** minimal test with temp SQLite (migrations applied), seed `homes` + two `subscribers`, insert one `events` row, **`mock()` `fetch`**, call `deliverEventToSubscribers`, assert **two** `fetch` calls and **two** `event_deliveries` rows — proves fan-out in isolation before HTTP wiring (Task 2).

</action>
  <verify>
    <automated>bun test src/webhooks/fan-out.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/webhooks/fan-out.test.ts` exits 0.
    - `rg "allSettled" src/webhooks/fan-out.ts` exits 0.
    - `rg "event_deliveries" src/webhooks/fan-out.ts` exits 0.
    - `rg "fetch" src/webhooks/fan-out.ts` exits 0.
  </acceptance_criteria>
  <done>Fan-out function exists; uses parameterized queries; records `event_deliveries`; uses `allSettled`.</done>
</task>

<task type="auto">
  <name>Task 2: Bun.serve routes for events and subscribers</name>
  <read_first>
    - src/types/events-api.ts
    - src/db/database.ts
    - .planning/phases/02-webhook-orchestrator/02-RESEARCH.md
    - src/webhooks/fan-out.ts
  </read_first>
  <files>src/server.ts, src/routes/events.ts, src/routes/subscribers.ts, index.ts, package.json</files>
  <action>

**Environment:** Read **`SQLITE_PATH`** with fallback to Phase 1 default (`data/home-assist.sqlite` per D-09). Read **`PORT`** with default **3000**. Open DB with FK pragma (reuse Phase 1 open helper).

**`POST /events`:** `await req.json()` inside `try/catch` — **400** on malformed JSON. Validate shape: `home_id` is integer, `event_type` is non-empty string, optional `body` any JSON-serializable — matches **`PostEventBody`**. Insert into **`events`** with parameterized INSERT; if FK fails (bad `home_id`), return **404** or **400** with clear JSON error (FK + optional `SELECT` existence check — be consistent).

On success return **201** + JSON `{ id: number, ... }` including the new event id. Then **`await deliverEventToSubscribers(...)`** per Task 1 ordering.

**`GET /events`:** Query param `home_id` (required) — return JSON array of events for that home (newest first recommended), parameterized `WHERE home_id = ?`.

**`POST /subscribers`:** Body `{ home_id, callback_url }` — validate URL string non-empty; optional lightweight URL parse (`URL` constructor) — **allow `http://127.0.0.1` and `http://localhost`** for lab per threat model. Insert into **`subscribers`**. Return **201** with subscriber id.

**`src/server.ts`:** Export `createServer(options?: { port?: number })` returning `{ server, url }` or similar so tests can pass **`port: 0`** for ephemeral port — read assigned port from the server object Bun exposes (use official Bun pattern for dynamic port).

**`index.ts`:** Import and start server for `bun run` dev.

**`package.json`:** Add `"dev"` / `"start"` scripts pointing at server entry if missing.

Use **`Bun.serve` `routes`** map for path/method dispatch; fallback `fetch` returns **404**.

</action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Bun\\.serve" src/server.ts` exits 0.
    - `rg "POST.*/events|/events" src/routes/events.ts` exits 0 (or equivalent route path in server.ts).
    - `rg "subscribers" src/routes/subscribers.ts` exits 0.
    - `bun test` exits 0.
  </acceptance_criteria>
  <done>All routes respond; POST /events and POST /subscribers persist; GET lists events by `home_id`.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — ephemeral port + mock fetch (HOOK-04 acceptance)</name>
  <read_first>
    - src/server.ts
    - src/webhooks/fan-out.test.ts
    - .planning/codebase/TESTING.md
  </read_first>
  <files>src/orchestrator.integration.test.ts</files>
  <action>

**Setup:** In `beforeAll`/`beforeEach`, apply migrations the same way Phase 1 tests do (reuse helper). Insert a **real `homes` row** (and any Phase 1 prerequisite) so `home_id` is FK-valid.

**Server:** Start orchestrator with **`port: 0`**, resolve base URL (e.g. from `server.url` or documented Bun API).

**HOOK-01/02:** POST `/events` with valid `PostEventBody` → expect **201**, parse `id`. GET `/events?home_id=` → includes that event.

**HOOK-03:** POST `/subscribers` twice with same `home_id`, two distinct `callback_url` strings (e.g. `http://127.0.0.1:9/a` and `http://127.0.0.1:9/b` — ports need not be listening when fetch is mocked).

**HOOK-04:** **`mock(module, "fetch", spy)`** or **`globalThis.fetch = mock(...)`** per `bun:test` docs — restore after test. POST `/events` once. **Assert `fetch` mock call count === 2** and each call’s URL/body matches subscribers. Assert **`event_deliveries`** has **2 rows** for that `event_id` (or equivalent query).

**Full suite:** `bun test` must pass.

</action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun test` exits 0.
    - `rg "mock" src/orchestrator.integration.test.ts` exits 0.
    - `rg "fetch" src/orchestrator.integration.test.ts` exits 0.
    - `rg "home_id" src/orchestrator.integration.test.ts` exits 0.
  </acceptance_criteria>
  <done>Single test file proves HOOK-01–HOOK-04; mock-based HOOK-04 acceptance criteria met; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `POST /events`, `POST /subscribers` | Untrusted JSON; validate shape and types |
| Orchestrator → subscriber `callback_url` | **SSRF:** server-side `fetch` to user-supplied URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | S | `callback_url` → internal IPs / metadata | **accept (lab)** | Document in code/README: **lab trusts subscriber URLs**; **`http://localhost` / `http://127.0.0.1` allowed** for local testing. Optional prod hardening deferred. |
| T-02-04 | E | SSRF to cloud metadata | **accept (lab)** | Same as above; no network ACL in v1 |
| T-02-05 | I | SQL injection in handlers | **mitigate** | **Only parameterized queries** — `$param` / prepared statements for all dynamic values; never `"... " + userInput` |
| T-02-06 | T | Tampered JSON body | **mitigate** | Validate `PostEventBody` shape; reject oversized bodies if adding a simple byte limit (optional) |

## SSRF (explicit)

- **Risk:** Attacker registers `callback_url` pointing at internal services.
- **Disposition:** **Accepted for lab** per `REQUIREMENTS.md` prototype scope; **local URLs explicitly allowed** for development. Document that **trusted subscribers only** applies to real deployments.

## SQLi (explicit)

- **Mitigation:** Parameterized SQLite access for every query touching user data.

</threat_model>

<verification>
- `bun test` green.
- Manual spot-check optional: `bun run` server, `curl` POST `/events` (not required for plan completion if tests cover behavior).
</verification>

<success_criteria>
- Locked **`PostEventBody`** enforced on POST `/events`.
- HOOK-04: **two** subscribers ⇒ **two** mocked `fetch` invocations + delivery records.
- `PORT` default 3000; `SQLITE_PATH` honored.
</success_criteria>

<output>
After completion, create `.planning/phases/02-webhook-orchestrator/02-02-SUMMARY.md`
</output>
