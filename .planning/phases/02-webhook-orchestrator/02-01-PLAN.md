---
phase: 02-webhook-orchestrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/002_events_subscribers.sql
  - src/db/migrate.ts
  - src/db/database.ts
  - src/types/events-api.ts
autonomous: true
requirements:
  - HOOK-02
  - HOOK-03
user_setup: []
must_haves:
  truths:
    - "SQLite contains `events`, `subscribers`, and `event_deliveries` tables with FK to `homes(id)`"
    - "Migration runner applies `002_events_subscribers.sql` after Phase 1 `001_initial.sql` without error"
    - "`PostEventBody` is exported for Phase 2 HTTP handlers (locked JSON contract)"
  artifacts:
    - path: "src/db/migrations/002_events_subscribers.sql"
      provides: "DDL for events, subscribers, optional delivery audit"
      min_lines: 20
    - path: "src/types/events-api.ts"
      provides: "PostEventBody + related request types"
      contains: "PostEventBody"
  key_links:
    - from: "src/db/migrate.ts"
      to: "src/db/migrations/002_events_subscribers.sql"
      via: "sequential apply after 001"
      pattern: "002_events_subscribers"
---

<objective>
Add Phase 2 schema and API contracts: versioned migration `002_events_subscribers.sql`, extend the Phase 1 migration runner, export the locked **`PostEventBody`** shape, and prove the repo still migrates and tests pass.

Purpose: Gives Wave 2 HTTP/fan-out work a stable DDL + type surface without mixing unrelated server code.

Output: New migration file, updated `migrate.ts` / `database.ts`, `src/types/events-api.ts`, green test run after blocking gate.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webhook-orchestrator/02-RESEARCH.md
@.planning/phases/01-data-model-sqlite/01-CONTEXT.md

Inherited Phase 1: `homes.id` is `INTEGER PRIMARY KEY AUTOINCREMENT` — all new `home_id` columns reference `homes(id)` with FK enabled (`PRAGMA foreign_keys=ON` on open). [D-04, D-07–D-10]

## Source coverage (wave 1)

| Source | Item | Covered by this plan |
|--------|------|----------------------|
| GOAL | Ingest/persist/fan-out foundation | Schema for persist + subscribers |
| REQ | HOOK-02, HOOK-03 | Tables + contracts |
| RESEARCH | events / subscribers / event_deliveries DDL | `002_events_subscribers.sql` |
| CONTEXT | D-07–D-08 migrations | New numbered SQL file + runner |
</context>

<tasks>

<task type="auto">
  <name>Task 1: DDL for events, subscribers, and event_deliveries</name>
  <read_first>
    - .planning/phases/02-webhook-orchestrator/02-RESEARCH.md (events/subscribers DDL)
    - src/db/migrations/001_initial.sql (timestamp and FK style to match)
    - .planning/phases/01-data-model-sqlite/01-CONTEXT.md (D-07, homes FK)
  </read_first>
  <files>src/db/migrations/002_events_subscribers.sql</files>
  <action>Create `002_events_subscribers.sql` (next file after `001_initial.sql`). Include:

- **`events`:** `id INTEGER PRIMARY KEY AUTOINCREMENT`, `home_id INTEGER NOT NULL REFERENCES homes(id) ON DELETE CASCADE` (or RESTRICT if Phase 1 prefers — match Phase 1 style), `event_type TEXT NOT NULL`, `body TEXT` (nullable JSON string for optional payload), `created_at` (TEXT ISO or INTEGER unix — match `001_initial.sql` timestamp style).
- **`subscribers`:** `id INTEGER PRIMARY KEY AUTOINCREMENT`, `home_id NOT NULL REFERENCES homes(id)`, `callback_url TEXT NOT NULL`, `created_at`, and **`UNIQUE(home_id, callback_url)`** so upsert-by-url is natural.
- **`event_deliveries`:** `id`, `event_id NOT NULL REFERENCES events(id)`, `subscriber_id NOT NULL REFERENCES subscribers(id)`, `status TEXT NOT NULL` (e.g. attempted/succeeded/failed), `http_status INTEGER`, `error_text TEXT`, `created_at` — satisfies roadmap “recorded deliveries” for HOOK-04 observability.

Add indexes appropriate for `WHERE home_id = ?` on `events` and `subscribers`. Use only static DDL (no string concatenation from user input in SQL files — SQLi N/A at DDL layer).

Do not implement HTTP in this task.
</action>
  <verify>
    <automated>grep -n "CREATE TABLE" src/db/migrations/002_events_subscribers.sql | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `rg "CREATE TABLE events" src/db/migrations/002_events_subscribers.sql` exits 0.
    - `rg "CREATE TABLE subscribers" src/db/migrations/002_events_subscribers.sql` exits 0.
    - `rg "CREATE TABLE event_deliveries" src/db/migrations/002_events_subscribers.sql` exits 0.
    - `rg "REFERENCES homes" src/db/migrations/002_events_subscribers.sql` exits 0.
  </acceptance_criteria>
  <done>File exists; three tables defined; FK clauses reference `homes(id)` and cross-table FKs as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Locked API types + wire migration into runner and DB open</name>
  <read_first>
    - src/db/migrate.ts
    - src/db/database.ts
    - src/db/migrations/002_events_subscribers.sql
  </read_first>
  <files>src/types/events-api.ts, src/db/migrate.ts, src/db/database.ts</files>
  <action>

**Types (`src/types/events-api.ts`):** Export the locked contract (align names with `02-RESEARCH.md` `PostEventBody`):

```typescript
export type PostEventBody = {
  home_id: number;
  event_type: string;
  body?: unknown;
};
```

Add `PostSubscriberBody` (or equivalent) with `home_id: number` and `callback_url: string` for HOOK-03 handlers in plan `02-02`.

**Migrations:** Extend `src/db/migrate.ts` so the ordered list of migration files includes the new `002_events_subscribers.sql` after `001_initial.sql` (same discovery/ordering pattern Phase 1 uses — do not fork semantics). [D-07, D-08]

**Database:** In `src/db/database.ts` (or equivalent open helper), ensure **`db.run("PRAGMA foreign_keys = ON;")`** runs when the connection is created for app and tests (per RESEARCH pitfall — invalid `home_id` must fail at FK layer).

</action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export type PostEventBody" src/types/events-api.ts` exits 0.
    - `rg "002_events_subscribers" src/db/migrate.ts` exits 0.
    - `rg "foreign_keys = ON" src/db/database.ts` exits 0.
    - `bun test` exits 0.
  </acceptance_criteria>
  <done>Types compile; migrate runner references `002_events_subscribers.sql`; FK pragma applied on DB open; existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Run migrations and full test suite</name>
  <read_first>
    - package.json (scripts: migrate, test)
    - src/db/migrate.ts
  </read_first>
  <files></files>
  <action>**[BLOCKING]** Before marking plan `02-01` complete:

1. Run the migration entrypoint the project uses (e.g. `bun run src/db/migrate.ts` or `bun run migrate` per `package.json`) against a disposable DB path via `SQLITE_PATH` (or in-memory if the runner supports it — prefer file path matching Phase 1 tests).
2. Run **`bun test`** from the repository root with the same env conventions Phase 1 tests use.

If either step fails, fix migration ordering, SQL syntax, or broken imports before completing this plan. Do not skip this gate.
</action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun test` exits 0.
    - `bun run migrate` exits 0 when that script exists (or `bun run src/db/migrate.ts` per package.json).
  </acceptance_criteria>
  <done>Migrations apply cleanly; `bun test` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL files → runner | Static DDL only; no runtime user input in DDL strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | Dynamic SQL in app queries | mitigate | Wave 2 handlers must use **parameterized** `db.query` / prepared statements with `$name` binds — document in `02-02-PLAN.md`; this plan only ships DDL |
| T-02-02 | T | Accidental FK off | mitigate | `PRAGMA foreign_keys=ON` on connection open (Task 2) |

## SQLi (explicit)

- **Risk:** SQL injection when building queries in TypeScript.
- **This plan:** DDL files are static. **Wave 2** must never interpolate user-controlled strings into SQL — use bound parameters for `home_id`, `event_type`, URLs, etc.

## SSRF (explicit)

- **Risk:** Not introduced in this plan (no `fetch`). Documented in `02-02-PLAN.md` for subscriber callback URLs.

</threat_model>

<verification>
- `002_events_subscribers.sql` present; migrate applies 001 then 002.
- `PostEventBody` exported exactly as locked.
- **Gate:** `bun test` green after migrations.
</verification>

<success_criteria>
Schema matches RESEARCH (`events`, `subscribers`, `event_deliveries`); FK to `homes(id)`; contracts exported; CI-local `bun test` passes after blocking step.
</success_criteria>

<output>
After completion, create `.planning/phases/02-webhook-orchestrator/02-01-SUMMARY.md`
</output>
