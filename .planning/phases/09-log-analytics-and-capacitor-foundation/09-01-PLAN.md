---
phase: 09-log-analytics-and-capacitor-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/events-analytics-api.ts
  - src/db/event-analytics.ts
  - src/routes/events-analytics.ts
  - src/server.ts
  - src/routes/events-analytics.test.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
must_haves:
  truths:
    - "User-triggered analytics requests return chart-ready aggregates over the selected time range."
    - "Analytics responses include volume trend, event-type distribution, and confidence trend in one contract."
    - "Unauthorized or malformed analytics requests fail with explicit 4xx errors and no raw SQL leakage."
  artifacts:
    - path: "src/routes/events-analytics.ts"
      provides: "GET /events/analytics handler with input validation and aggregate response shaping"
      exports: ["handleGetEventsAnalytics"]
    - path: "src/db/event-analytics.ts"
      provides: "SQLite aggregate query helpers for trend buckets and type counts"
      exports: ["queryEventVolumeTrend", "queryEventTypeDistribution", "queryConfidenceTrend"]
    - path: "src/types/events-analytics-api.ts"
      provides: "Shared analytics request/response contracts"
      exports: ["EventsAnalyticsQuery", "EventsAnalyticsResponse"]
  key_links:
    - from: "src/server.ts"
      to: "src/routes/events-analytics.ts"
      via: "routeRequest branch for /events/analytics"
      pattern: "path === \"/events/analytics\""
    - from: "src/routes/events-analytics.ts"
      to: "src/db/event-analytics.ts"
      via: "validated query args to aggregate helpers"
      pattern: "queryEventVolumeTrend|queryEventTypeDistribution|queryConfidenceTrend"
---

<objective>
Create the backend analytics contract and aggregation endpoint that powers Recharts log visualizations.

Purpose: Provide stable, reusable, chart-friendly event analytics for UI without embedding SQL logic in the client.
Output: Typed analytics API contracts, SQLite aggregation helper module, route wiring, and regression tests.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/routes/events.ts
@src/server.ts
@src/client/api/events-client.ts
@src/db/database.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define analytics API contracts and aggregate query module</name>
  <files>src/types/events-analytics-api.ts, src/db/event-analytics.ts, src/routes/events-analytics.test.ts</files>
  <behavior>
    - Test 1: Invalid bucket query values are rejected with `400`.
    - Test 2: Aggregate helpers return deterministic bucket rows for seeded events.
    - Test 3: Confidence trend excludes rows with missing confidence fields.
  </behavior>
  <action>Create `src/types/events-analytics-api.ts` with explicit query inputs (`location_id`, `range`, `bucket`, optional `event_type`) and response sections (`volumeTrend`, `eventTypeDistribution`, `confidenceTrend`, `totals`). Implement `src/db/event-analytics.ts` with prepared SQL statements and parameter binding for hourly/daily bucketing, event type grouping, and confidence extraction from JSON payload fields (`confidence` or `match_score`). Seed-oriented analytics route test scaffold should be added in `src/routes/events-analytics.test.ts` so helper behavior can be verified before route wiring.</action>
  <verify>
    <automated>bun test src/routes/events-analytics.test.ts -t "aggregate helpers"</automated>
  </verify>
  <done>Shared analytics contracts and reusable DB aggregate helpers exist and are covered by deterministic test cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose GET /events/analytics with strict validation and route wiring</name>
  <files>src/routes/events-analytics.ts, src/server.ts</files>
  <behavior>
    - Test 1: Valid analytics request returns 200 with all response sections present.
    - Test 2: Missing actor (`x-user-id`) returns a 400 error.
    - Test 3: Unauthorized location access (`location_id` provided but inaccessible) returns 403 with generic access-denied payload.
  </behavior>
  <action>Implement `handleGetEventsAnalytics(req, db)` in `src/routes/events-analytics.ts` following existing `json()` + parse + guard style from `src/routes/events.ts`. Validate query values (`range` in allowed presets, `bucket` in allowed presets). Treat `location_id` as optional: when absent, aggregate all events accessible to `x-user-id` (existing Events page in global context); when present, enforce membership check and aggregate only that location (same Events page in location context). Add route branch in `src/server.ts` for `GET /events/analytics` and CORS OPTIONS handling parity with existing event/location endpoints.</action>
  <verify>
    <automated>bun test src/routes/events-analytics.test.ts -t "GET /events/analytics"</automated>
  </verify>
  <done>Analytics route is reachable from server, validates inputs, enforces scope, and returns chart-ready aggregate JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Lock analytics API behavior with integration coverage</name>
  <files>src/routes/events-analytics.test.ts</files>
  <action>Expand `src/routes/events-analytics.test.ts` to include seeded event fixtures across multiple timestamps/types/confidence values and assert response math for totals, distribution percentages, and trend ordering. Include one regression asserting stable empty-state payloads (arrays empty, totals zero) to prevent UI null-check breakage.</action>
  <verify>
    <automated>bun test src/routes/events-analytics.test.ts</automated>
  </verify>
  <done>Automated tests provide confidence that analytics responses remain stable for UI chart consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→analytics API | Untrusted query params and actor headers enter aggregate query path |
| analytics route→SQLite | Validated filters translate into SQL group-by queries |
| JSON payload confidence parsing | Untrusted event body JSON is interpreted for confidence trend metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | analytics query params | mitigate | Whitelist `range` and `bucket` values; reject unknown values with 400 |
| T-09-02 | I | cross-location analytics access | mitigate | Enforce location membership check via `x-user-id` before aggregating |
| T-09-03 | T | SQL execution | mitigate | Use prepared statements and bound parameters only in `src/db/event-analytics.ts` |
| T-09-04 | R | aggregate output integrity | mitigate | Add integration tests with seeded fixtures and expected totals/order |
</threat_model>

<verification>
Run `bun test src/routes/events-analytics.test.ts` and confirm analytics contract tests pass.
</verification>

<success_criteria>
Backend exposes a validated, scoped analytics endpoint that returns all chart data required by Phase 9 UI.
</success_criteria>

<output>
After completion, create `.planning/phases/09-log-analytics-and-capacitor-foundation/09-01-SUMMARY.md`
</output>
