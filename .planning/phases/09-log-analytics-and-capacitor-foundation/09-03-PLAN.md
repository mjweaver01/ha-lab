---
phase: 09-log-analytics-and-capacitor-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - package.json
  - capacitor.config.ts
  - ios/App/App/capacitor.config.json
  - android/app/src/main/assets/capacitor.config.json
  - README.md
autonomous: false
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
  - CAP-04
user_setup:
  - service: ios-toolchain
    why: "Capacitor iOS shell build and simulator launch"
    dashboard_config:
      - task: "Install Xcode and command-line tools"
        location: "Local macOS development machine"
  - service: android-toolchain
    why: "Capacitor Android shell build and emulator launch"
    dashboard_config:
      - task: "Install Android Studio with SDK and emulator image"
        location: "Local macOS development machine"
must_haves:
  truths:
    - "Capacitor configuration points at the built web assets for this Bun/React app."
    - "iOS and Android project shells can be generated and synced from the same source."
    - "Developers have documented commands to rebuild web assets and re-sync native shells."
  artifacts:
    - path: "capacitor.config.ts"
      provides: "Capacitor app id/name + webDir + server behavior configuration"
      contains: "webDir"
    - path: "package.json"
      provides: "repeatable mobile scripts for sync and native open/run"
      contains: "mobile:sync"
    - path: "README.md"
      provides: "developer workflow for Capacitor build/sync/run"
      contains: "Capacitor"
  key_links:
    - from: "package.json"
      to: "capacitor.config.ts"
      via: "build/sync scripts consuming configured webDir"
      pattern: "mobile:sync"
    - from: "capacitor.config.ts"
      to: "ios/App and android/app"
      via: "npx cap sync generated config propagation"
      pattern: "webDir"
---

<objective>
Integrate Capacitor runtime support so the existing web app can run in iOS/Android shells with repeatable developer workflows.

Purpose: Establish mobile shell foundations without disrupting current Bun+React browser workflows.
Output: Capacitor dependencies/config, generated platform projects, mobile scripts, and documented UAT flow.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-log-analytics-and-capacitor-foundation/09-02-PLAN.md
@package.json
@src/client/index.html
@src/client/main.tsx
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Capacitor dependencies, config, and mobile scripts</name>
  <files>package.json, capacitor.config.ts</files>
  <action>Install latest `@capacitor/core`, `@capacitor/cli`, `@capacitor/ios`, and `@capacitor/android`. Add scripts for `build:web:mobile` (static web build output to `dist/client`), `mobile:sync`, `mobile:ios`, and `mobile:android`. Create `capacitor.config.ts` with appId (`lab.homeassist.app`, unless project chooses a different reverse-domain value) and appName (`Home Assistant Lab`) plus `webDir: "dist/client"` and conservative defaults for local development. Keep existing `dev`, `client`, and test scripts unchanged.</action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Project can compile with Capacitor dependencies and has canonical scripts/config for mobile sync flow.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and sync native platform projects</name>
  <files>ios/App/App/capacitor.config.json, android/app/src/main/assets/capacitor.config.json, README.md</files>
  <action>Run mobile build + platform generation workflow: build web assets, run `npx cap add ios`, `npx cap add android`, then `npx cap sync`. Commit generated Capacitor config artifacts required for deterministic sync. Update `README.md` with a short "Capacitor mobile workflow" section that includes prerequisites, initial setup, re-sync command sequence, and smoke test commands.</action>
  <verify>
    <automated>bun run build:web:mobile && bun run mobile:sync</automated>
  </verify>
  <done>Native platform projects and synced config artifacts exist and can be refreshed with documented commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify native shell launch and route smoke</name>
  <action>Request user verification after automation completes because native simulator/device runtime validation cannot be fully asserted by CLI alone.</action>
  <what-built>Capacitor integration, native shell generation, and sync workflow automation for iOS/Android.</what-built>
  <how-to-verify>
    1) Run `bun run mobile:ios` and confirm the app opens in Xcode with the `Home Assistant Lab` web UI rendering.
    2) Run `bun run mobile:android` and confirm Android Studio opens and emulator launch shows the same UI shell.
    3) In both shells, navigate to Events and Locations tabs and confirm the app header/nav + events list render without white screen or route failures.
  </how-to-verify>
  <resume-signal>Type "approved" if both native shells launch correctly, or share the failing step/output.</resume-signal>
  <verify>
    <automated>bun run build:web:mobile && bun run mobile:sync</automated>
  </verify>
  <done>User confirms iOS and Android shells launch and core app routes render correctly in native WebViews.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web build output→native shell | Built web assets are embedded into native app containers |
| Capacitor bridge boundary | Native runtime can expose plugin surfaces to web code |
| developer scripts→generated projects | Script drift can create inconsistent iOS/Android project state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | T | Capacitor config/scripts | mitigate | Keep version-pinned scripts in `package.json` and commit config files for deterministic sync |
| T-09-10 | E | Capacitor plugin/runtime surface | mitigate | Start with core setup only; avoid adding privileged plugins in this phase |
| T-09-11 | D | stale web assets in native shells | mitigate | Enforce `build:web:mobile` before `mobile:sync` in docs and verification commands |
| T-09-12 | R | native smoke verification evidence | mitigate | Require human checkpoint verifying iOS/Android launch and core route rendering |
</threat_model>

<verification>
Run `bun run typecheck` and `bun run build:web:mobile && bun run mobile:sync` before requesting human native-shell validation.
</verification>

<success_criteria>
Capacitor is fully configured and repeatable scripts/docs exist so developers can launch the app in iOS/Android shells.
</success_criteria>

<output>
After completion, create `.planning/phases/09-log-analytics-and-capacitor-foundation/09-03-SUMMARY.md`
</output>
