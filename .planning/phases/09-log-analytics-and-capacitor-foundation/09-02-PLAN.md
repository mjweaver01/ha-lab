---
phase: 09-log-analytics-and-capacitor-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - package.json
  - src/client/api/events-analytics-client.ts
  - src/client/lib/events-analytics-view.ts
  - src/client/events-screen.tsx
  - src/client/styles.css
  - src/client/events-screen.analytics.test.tsx
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
must_haves:
  truths:
    - "User can see event-volume trend chart for selected time range and bucket settings."
    - "User can see event-type distribution and confidence trend from the same analytics response."
    - "Analytics controls update chart datasets without blocking existing event polling list."
  artifacts:
    - path: "src/client/events-screen.tsx"
      provides: "Analytics panel UI integrated with existing events controls"
      contains: "Analytics"
    - path: "src/client/api/events-analytics-client.ts"
      provides: "Typed client fetch wrapper for /events/analytics"
      exports: ["fetchEventsAnalytics"]
    - path: "src/client/events-screen.analytics.test.tsx"
      provides: "Recharts analytics behavior and fallback rendering tests"
      contains: "describe(\"events analytics panel\""
  key_links:
    - from: "src/client/events-screen.tsx"
      to: "src/client/api/events-analytics-client.ts"
      via: "analytics refresh effect"
      pattern: "fetchEventsAnalytics"
    - from: "src/client/events-screen.tsx"
      to: "src/client/lib/events-analytics-view.ts"
      via: "chart transform helpers"
      pattern: "toVolumeSeries|toDistributionSeries|toConfidenceSeries"
---

<objective>
Implement Recharts-powered analytics visualizations in the events UI using the Phase 9 analytics endpoint.

Purpose: Give users actionable trend visibility directly in the existing Events page while preserving existing list/polling usability.
Output: Recharts dependency, analytics client + transform layer, analytics panel UI, and regression tests.
</objective>

<execution_context>
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/michaelweaver/Websites/home-assist/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-log-analytics-and-capacitor-foundation/09-01-PLAN.md
@src/client/events-screen.tsx
@src/client/api/events-client.ts
@src/client/hooks/use-events-poll.ts
@src/client/styles.css
</context>

<interfaces>
From `src/client/api/events-client.ts`:
- `EventListItem` includes `id`, `location_id`, `event_type`, `created_at`, `body`.

From `src/client/hooks/use-events-poll.ts`:
- `UseEventsPollResult` provides `events`, `loading`, `error`, `newIds`, `onRefresh`, `locationId`.

Executor should preserve these contracts while layering analytics UI.

UI scope guardrail:
- Do **not** create a dedicated analytics page or route.
- Render analytics inline inside `src/client/events-screen.tsx`, with automatic scoping from current Events context.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Recharts dependency and typed analytics client</name>
  <files>package.json, src/client/api/events-analytics-client.ts, src/client/events-screen.analytics.test.tsx</files>
  <behavior>
    - Test 1: Analytics fetch wrapper parses valid response sections.
    - Test 2: Non-2xx analytics responses throw informative errors.
    - Test 3: Missing/invalid analytics payload shape throws deterministic parsing error.
  </behavior>
  <action>Add latest `recharts` dependency to `package.json`. Create `src/client/api/events-analytics-client.ts` mirroring fetch/error conventions in `events-client.ts`, with typed query builder and strict response parsing against `EventsAnalyticsResponse`. Add initial test scaffold in `src/client/events-screen.analytics.test.tsx` to verify parsing/error behavior before chart rendering integration.</action>
  <verify>
    <automated>bun test src/client/events-screen.analytics.test.tsx -t "analytics client"</automated>
  </verify>
  <done>Analytics client and chart dependency are in place with deterministic parsing/error handling tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build analytics transforms and render Recharts panel in Events screen</name>
  <files>src/client/lib/events-analytics-view.ts, src/client/events-screen.tsx, src/client/styles.css</files>
  <behavior>
    - Test 1: Default analytics panel renders volume trend chart with non-empty dataset.
    - Test 2: Range/bucket controls trigger data refresh and chart rerender.
    - Test 3: Empty analytics dataset shows explicit no-data state instead of blank chart.
  </behavior>
  <action>Create `src/client/lib/events-analytics-view.ts` to convert API payload sections into Recharts-friendly arrays and axis labels. Update `src/client/events-screen.tsx` to add a new `ui-panel` analytics section using `ResponsiveContainer` with `ComposedChart` for volume/confidence trends and `BarChart` for event-type distribution, plus controls for range and bucket. Maintain existing event list render path and polling cadence so analytics fetch runs independently and does not block list updates. Add minimal styles in `src/client/styles.css` for analytics grid/cards while reusing existing `ui-*` primitives.</action>
  <verify>
    <automated>bun test src/client/events-screen.analytics.test.tsx -t "charts render"</automated>
  </verify>
  <done>Events screen includes responsive analytics charts with live controls and resilient empty/error states.</done>
</task>

<task type="auto">
  <name>Task 3: Add analytics UI regression coverage</name>
  <files>src/client/events-screen.analytics.test.tsx</files>
  <action>Expand analytics test suite to cover loading state, API error banner, no-confidence fallback, range/bucket change handlers, and coexistence with existing events list controls (`Nice/JSON`, pagination, timeframe). Include assertions that chart panel still renders when events list is empty to prevent hidden-regression coupling.</action>
  <verify>
    <automated>bun test src/client/events-screen.analytics.test.tsx</automated>
  </verify>
  <done>Analytics behavior is protected by focused UI tests and does not regress existing events-screen functionality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics API response→UI | Untrusted JSON from API is parsed and rendered in chart components |
| user controls→analytics query | Range/bucket/event-type control values shape backend analytics requests |
| chart rendering path | Potential large datasets can degrade UI responsiveness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | T | analytics response parsing | mitigate | Validate response shape before rendering; reject malformed payload with explicit error state |
| T-09-06 | I | chart tooltip content | mitigate | Render tooltip values as plain text only; no HTML injection from payload values |
| T-09-07 | D | large analytics datasets | mitigate | Restrict range/bucket presets and cap datapoint count server-side; show bounded chart points |
| T-09-08 | R | analytics/event-list coupling regressions | mitigate | Add regression tests asserting existing events list controls still function with analytics enabled |
</threat_model>

<verification>
Run `bun test src/client/events-screen.analytics.test.tsx` and `bun test src/client/events-screen.test.tsx`.
</verification>

<success_criteria>
Events screen delivers chart-based log analytics with responsive controls and no regressions to core event list behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-log-analytics-and-capacitor-foundation/09-02-SUMMARY.md`
</output>
